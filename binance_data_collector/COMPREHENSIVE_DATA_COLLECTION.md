# 📊 Комплексный сбор всех данных с Binance

Это руководство описывает скрипт `collect_comprehensive_data.py`, который собирает **максимально полную историю всех доступных данных** с Binance и объединяет их в один DataFrame.

## 🎯 Что собирается

Скрипт собирает следующие типы данных:

1. **OHLCV данные** (Open, High, Low, Close, Volume)
   - Полная история с момента запуска Binance (август 2017)
   - Все доступные таймфреймы
   - Дополнительные метрики: quote_volume, taker_buy_base, taker_buy_quote, trades

2. **Агрегированные сделки** (Aggregated Trades)
   - Объемы рыночных покупок (market_buy_volume)
   - Объемы рыночных продаж (market_sell_volume)
   - Общее количество сделок
   - **Объемы торгов** (total_trade_quantity)

3. **Лимитные заявки (Order Book)** на глубинах 3%, 8%, 15%, 60%
   - Объемы лимитных заявок на покупку (bid_volume) для каждой глубины
   - Объемы лимитных заявок на продажу (ask_volume) для каждой глубины
   - Общий объем на глубине (total_volume)
   - Дисбаланс order book (imbalance)
   - ⚠️ **Важно**: Binance не предоставляет историю order book, поэтому собираются периодические снимки текущего состояния

4. **Ликвидации** (Liquidations)
   - Long ликвидации
   - Short ликвидации
   - Общий объем ликвидаций
   - ⚠️ Доступны только для фьючерсов, ограниченная история

5. **Открытый интерес** (Open Interest)
   - Открытый интерес в базовой валюте (BTC)
   - Стоимость открытого интереса в USDT

6. **Вычисляемые метрики**
   - Buy/Sell Ratio (соотношение покупок и продаж)
   - Liquidation Ratio (соотношение long/short ликвидаций)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Базовое использование

Сбор всех данных с максимальной историей (с 2017 года):

```bash
python scripts/collect_comprehensive_data.py
```

Это соберет:
- Данные для BTCUSDT
- С 2017-08-01 до сегодня
- Интервал агрегации: 15 минут

### 3. Настройка параметров

```bash
# Указать символ
python scripts/collect_comprehensive_data.py --symbol ETHUSDT

# Указать период
python scripts/collect_comprehensive_data.py \
    --start-date 2023-01-01 \
    --end-date 2024-12-31

# Изменить интервал
python scripts/collect_comprehensive_data.py \
    --interval 1h \
    --target-interval 1h

# Указать путь сохранения
python scripts/collect_comprehensive_data.py \
    --output data/my_comprehensive_data.csv
```

## 📋 Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--symbol` | Торговая пара (например, BTCUSDT) | BTCUSDT |
| `--start-date` | Начальная дата (YYYY-MM-DD) | 2017-08-01 |
| `--end-date` | Конечная дата (YYYY-MM-DD) | Сегодня |
| `--interval` | Интервал сбора OHLCV (1m, 5m, 15m, 1h, 1d) | 15m |
| `--target-interval` | Целевой интервал агрегации | 15m |
| `--output` | Путь для сохранения файла | auto (генерируется) |

## 💻 Использование в Python коде

```python
from datetime import datetime
from binance_data_collector.scripts.collect_comprehensive_data import ComprehensiveBinanceCollector

# Создаем коллектор
collector = ComprehensiveBinanceCollector(symbol="BTCUSDT")

# Собираем данные за последний год
start_date = datetime(2024, 1, 1)
end_date = datetime.now()

df = collector.collect_comprehensive_data(
    start_date=start_date,
    end_date=end_date,
    interval="15m",        # Минимальный интервал для сбора
    target_interval="1h"    # Финальный интервал агрегации
)

# Работа с данными
print(f"Всего записей: {len(df)}")
print(f"Колонки: {list(df.columns)}")
print(df.head())

# Анализ данных
df['price_change'] = df['close'].pct_change()
df['volume_trend'] = df['volume'].rolling(24).mean()
print(df[['close', 'price_change', 'volume_trend']].tail(10))
```

## 📊 Структура итогового DataFrame

### Базовые колонки (OHLCV):
- `open` - цена открытия
- `high` - максимальная цена
- `low` - минимальная цена
- `close` - цена закрытия
- `volume` - объем в базовой валюте
- `quote_volume` - объем в котируемой валюте (USDT)
- `taker_buy_base` - объем покупок тейкерами
- `taker_buy_quote` - стоимость покупок тейкерами
- `trades` - количество сделок

### Рыночные объемы:
- `market_buy_volume` - объем рыночных покупок
- `market_sell_volume` - объем рыночных продаж
- `total_trade_quantity` - общий объем сделок (объемы торгов)
- `last_trade_price` - цена последней сделки

### Лимитные заявки (Order Book) для каждой глубины (3%, 8%, 15%, 60%):
- `bid_volume_3pct` - объем лимитных заявок на покупку на глубине 3%
- `ask_volume_3pct` - объем лимитных заявок на продажу на глубине 3%
- `total_volume_3pct` - общий объем лимитных заявок на глубине 3%
- `imbalance_3pct` - дисбаланс order book на глубине 3% ((bid - ask) / total)
- Аналогично для `8pct`, `15pct`, `60pct`

### Ликвидации:
- `long_liquidations` - объем ликвидированных long позиций
- `short_liquidations` - объем ликвидированных short позиций
- `total_liquidations` - общий объем ликвидаций

### Открытый интерес:
- `open_interest` - открытый интерес в BTC
- `open_interest_value` - стоимость открытого интереса в USDT

### Вычисляемые метрики:
- `buy_sell_ratio` - соотношение покупок к продажам
- `liquidation_ratio` - соотношение long к short ликвидациям

## ⚙️ Оптимизация производительности

### Сбор данных по частям

Для очень больших периодов рекомендуется собирать данные по частям:

```python
from datetime import datetime, timedelta

collector = ComprehensiveBinanceCollector(symbol="BTCUSDT")

# Собираем по годам
start = datetime(2017, 8, 1)
end = datetime.now()

all_dataframes = []
current = start

while current < end:
    year_end = min(current + timedelta(days=365), end)
    
    print(f"Сбор данных с {current.date()} по {year_end.date()}...")
    df = collector.collect_comprehensive_data(
        start_date=current,
        end_date=year_end,
        interval="1h",
        target_interval="1h"
    )
    
    if not df.empty:
        all_dataframes.append(df)
    
    current = year_end

# Объединяем все данные
import pandas as pd
final_df = pd.concat(all_dataframes)
final_df.sort_index(inplace=True)
final_df = final_df[~final_df.index.duplicated(keep='first')]
```

### Выбор интервала

- **Для детального анализа**: используйте `--interval 1m` или `--interval 5m`
- **Для долгосрочного анализа**: используйте `--interval 1h` или `--interval 1d`
- **Для быстрого сбора**: используйте большие интервалы (`1h`, `1d`)

## ⚠️ Важные замечания

### Ограничения API Binance:

1. **OHLCV данные**: ✅ Полная история доступна
2. **Агрегированные сделки (рыночные покупки/продажи)**: ✅ Полная история доступна
3. **Лимитные заявки (Order Book)**: ⚠️ **Исторические данные недоступны**
   - Binance не предоставляет историю order book через REST API
   - Скрипт собирает периодические снимки текущего состояния order book во время сбора данных
   - Частота снимков зависит от объема данных (автоматически определяется)
   - Для получения точных данных order book в реальном времени используйте WebSocket
4. **Ликвидации**: ⚠️ Доступны только для фьючерсов, ограниченная история (обычно несколько месяцев)
5. **Открытый интерес**: ✅ История доступна через специальный endpoint

### Время выполнения:

- Сбор данных за 1 год с интервалом 15m: ~15-30 минут
- Сбор данных за все время (2017-2024) с интервалом 1h: ~1-2 часа
- Сбор данных с интервалом 1m за длительный период: может занять несколько часов

### Лимиты API:

- Скрипт автоматически соблюдает лимиты API (1200 запросов/минуту)
- При ошибках rate limit автоматически увеличивается задержка
- Рекомендуется собирать данные в непиковые часы

## 📈 Примеры анализа данных

### Анализ волатильности

```python
df['volatility'] = df['close'].rolling(24).std()
df['volatility_percent'] = (df['volatility'] / df['close']) * 100
```

### Корреляция между метриками

```python
correlations = df[['close', 'volume', 'total_liquidations', 'open_interest']].corr()
print(correlations)
```

### Анализ дисбаланса покупок/продаж

```python
df['buy_pressure'] = df['market_buy_volume'] / (df['market_buy_volume'] + df['market_sell_volume'])
df['sell_pressure'] = df['market_sell_volume'] / (df['market_buy_volume'] + df['market_sell_volume'])
```

## 🐛 Решение проблем

### Ошибка "Rate limit exceeded"

Скрипт автоматически обрабатывает эту ошибку, увеличивая задержку. Если проблема сохраняется:

1. Увеличьте `REQUEST_DELAY` в `config/config.py`
2. Собирайте данные по меньшим периодам
3. Используйте большие интервалы

### Пустые данные по ликвидациям

- Ликвидации доступны только для фьючерсов
- Для старых периодов данные могут отсутствовать
- Это нормально - скрипт продолжит работу без них

### Нет данных открытого интереса

- Проверьте, что используется правильный символ (BTCUSDT)
- Для очень старых данных может не быть истории
- Скрипт заполнит нулями и продолжится

## 📚 Дополнительная информация

- [Документация Binance API](https://binance-docs.github.io/apidocs/spot/en/)
- [Документация Binance Futures API](https://binance-docs.github.io/apidocs/futures/en/)
- [Документация pandas](https://pandas.pydata.org/docs/)

## ✅ Готово!

Теперь у вас есть все необходимые инструменты для сбора максимально полной истории данных с Binance!

