# 🚀 Быстрый старт

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. (Опционально) Настройте API ключи:
```bash
cp config/.env.example config/.env
# Отредактируйте config/.env и добавьте свои ключи
```

## Использование

### 📊 Сбор исторических данных

Запустите скрипт для сбора исторических данных:
```bash
python scripts/collect_historical.py
```

Скрипт автоматически:
- Соберет данные для BTC/USDT
- Для таймфреймов: 15m, 1h, 4h, 1d
- Сохранит данные в `data/historical/`

### 📡 Данные в реальном времени

Для получения данных в реальном времени:
```bash
python scripts/realtime_stream.py
```

Скрипт будет:
- Подключаться к WebSocket Binance
- Собирать данные свечей в реальном времени
- Сохранять данные в `data/realtime/`

Нажмите `Ctrl+C` для остановки.

### 📤 Экспорт данных

Конвертация собранных данных в другой формат:
```bash
# Конвертировать все CSV в Parquet
python scripts/export_data.py --input-dir data/historical --output-format parquet

# Конвертировать в Excel
python scripts/export_data.py --input-dir data/historical --output-format xlsx
```

## Пример использования в Python

```python
from binance_data_collector.config import config
from binance_data_collector.utils.file_handler import load_data
import pandas as pd

# Загрузка данных
filepath = config.DATA_DIR / "historical" / "BTCUSDT_1h_20240101_20241231.csv"
df = load_data(filepath)

# Работа с данными
print(df.head())
print(f"Всего записей: {len(df)}")
```

## Настройка параметров

Отредактируйте `config/config.py` для изменения:
- Торговых пар (DEFAULT_SYMBOL)
- Таймфреймов (DEFAULT_TIMEFRAMES)
- Путей сохранения данных
- Лимитов API


