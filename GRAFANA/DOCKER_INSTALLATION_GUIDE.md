# 🐳 РУКОВОДСТВО ПО УСТАНОВКЕ DOCKER ДЛЯ WINDOWS

## 📋 ЧТО ТАКОЕ DOCKER?

Docker - это платформа для контейнеризации приложений. В нашем случае он нужен для запуска:
- **InfluxDB** - база данных для временных рядов
- **Grafana** - платформа для визуализации

Оба приложения будут работать в изолированных контейнерах.

---

## ✅ ПРОВЕРКА УСТАНОВКИ

Перед установкой проверьте, может быть Docker уже установлен:

```powershell
docker --version
docker-compose --version
```

Если команды работают, Docker уже установлен! Переходите к разделу "Настройка Docker".

---

## 📥 УСТАНОВКА DOCKER DESKTOP ДЛЯ WINDOWS

### Шаг 1: Требования системы

Убедитесь, что ваша система соответствует требованиям:
- **Windows 10/11** (64-bit) версия 2004 или выше
- **WSL 2** (Windows Subsystem for Linux) - обычно устанавливается автоматически
- **Включена виртуализация** в BIOS (Intel VT-x или AMD-V)

### Шаг 2: Скачивание Docker Desktop

1. Перейдите на официальный сайт: https://www.docker.com/products/docker-desktop/
2. Нажмите **"Download for Windows"**
3. Скачайте файл `Docker Desktop Installer.exe` (размер ~500 MB)

### Шаг 3: Установка

1. **Запустите установщик** `Docker Desktop Installer.exe`

2. **Важные настройки во время установки:**
   - ✅ Отметьте **"Use WSL 2 instead of Hyper-V"** (если доступно)
   - ✅ Отметьте **"Add shortcut to desktop"** (для удобства)
   - ✅ Отметьте **"Start Docker Desktop when you log in"** (опционально)

3. **Нажмите "Ok"** и дождитесь установки

4. После установки **перезагрузите компьютер** (если потребуется)

### Шаг 4: Запуск Docker Desktop

1. **Запустите Docker Desktop** с рабочего стола или из меню Пуск

2. **При первом запуске:**
   - Примите условия лицензии
   - Docker Desktop начнет инициализацию (может занять 1-2 минуты)
   - Может появиться окно с запросом на включение WSL 2 - нажмите **"Install"**

3. **Дождитесь полного запуска:**
   - В системном трее (правый нижний угол) появится иконка Docker
   - Иконка должна быть зеленой/синей (не красной)
   - Откройте Docker Desktop - должно быть написано "Docker Desktop is running"

### Шаг 5: Проверка установки

Откройте PowerShell или Command Prompt и выполните:

```powershell
docker --version
```

Ожидаемый результат:
```
Docker version 24.x.x, build xxxxx
```

Проверьте также:
```powershell
docker-compose --version
```

Ожидаемый результат:
```
Docker Compose version v2.x.x
```

Запустите тестовый контейнер:
```powershell
docker run hello-world
```

Если увидите сообщение "Hello from Docker!" - всё работает правильно! ✅

---

## ⚙️ НАСТРОЙКА DOCKER

### 1. Настройки ресурсов

Docker Desktop может потреблять много ресурсов. Настройте лимиты:

1. Откройте **Docker Desktop**
2. Перейдите в **Settings** (⚙️) → **Resources**
3. Настройте:
   - **CPUs**: минимум 2, рекомендуется 4
   - **Memory**: минимум 4 GB, рекомендуется 8 GB
   - **Disk image size**: минимум 20 GB
4. Нажмите **"Apply & Restart"**

### 2. Настройка WSL 2 (если используется)

1. **Settings** → **General**
2. Отметьте **"Use the WSL 2 based engine"**
3. Перейдите в **Resources** → **WSL Integration**
4. Включите интеграцию с нужными дистрибутивами WSL
5. Нажмите **"Apply & Restart"**

### 3. Настройка прокси (если нужно)

Если вы используете прокси-сервер:
1. **Settings** → **Resources** → **Proxies**
2. Укажите настройки прокси
3. Нажмите **"Apply & Restart"**

---

## 🐛 РЕШЕНИЕ ТИПИЧНЫХ ПРОБЛЕМ

### Проблема 1: "WSL 2 installation is incomplete"

**Решение:**
1. Откройте PowerShell **от имени администратора**
2. Выполните:
   ```powershell
   wsl --update
   wsl --set-default-version 2
   ```
3. Перезагрузите компьютер
4. Повторно запустите Docker Desktop

### Проблема 2: "Virtualization is not enabled"

**Решение:**
1. Перезагрузите компьютер
2. Войдите в BIOS/UEFI (обычно F2, F12, Del при загрузке)
3. Найдите опцию **Virtualization** или **Intel Virtualization Technology**
4. Включите её (Enable)
5. Сохраните и перезагрузите компьютер

### Проблема 3: Docker Desktop не запускается

**Решение:**
1. Проверьте, что все процессы Docker завершены:
   ```powershell
   taskkill /F /IM "Docker Desktop.exe"
   taskkill /F /IM "com.docker.backend.exe"
   ```
2. Перезапустите Docker Desktop
3. Если не помогает - переустановите Docker Desktop

### Проблема 4: "Port already in use"

Если порты 8086 (InfluxDB) или 3000 (Grafana) заняты:

**Проверьте, что порты свободны:**
```powershell
netstat -ano | findstr :8086
netstat -ano | findstr :3000
```

**Остановите процесс, использующий порт:**
```powershell
taskkill /PID <номер_процесса> /F
```

Или измените порты в `docker-compose.yml`:
```yaml
ports:
  - "8087:8086"  # Измените внешний порт
  - "3001:3000"
```

---

## ✅ ПРОВЕРКА ГОТОВНОСТИ

После установки и настройки выполните финальную проверку:

```powershell
# 1. Проверка версии Docker
docker --version

# 2. Проверка версии Docker Compose
docker-compose --version

# 3. Тестовый контейнер
docker run hello-world

# 4. Проверка запущенных контейнеров
docker ps

# 5. Проверка образов
docker images
```

Если все команды выполняются без ошибок - Docker готов к работе! ✅

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

После успешной установки Docker:

1. **Переходите к запуску Grafana:**
   ```powershell
   cd GRAFANA/grafana
   docker-compose up -d
   ```

2. **Следуйте инструкциям из `QUICK_START_15M.md`**

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- [Официальная документация Docker](https://docs.docker.com/desktop/install/windows-install/)
- [Docker Desktop User Guide](https://docs.docker.com/desktop/)
- [Troubleshooting Docker Desktop](https://docs.docker.com/desktop/troubleshoot/overview/)

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ DOCKER

```powershell
# Просмотр запущенных контейнеров
docker ps

# Просмотр всех контейнеров (включая остановленные)
docker ps -a

# Просмотр логов контейнера
docker logs <container_name>

# Остановка контейнера
docker stop <container_name>

# Удаление контейнера
docker rm <container_name>

# Просмотр образов
docker images

# Удаление образа
docker rmi <image_name>

# Остановка всех контейнеров
docker stop $(docker ps -aq)

# Удаление всех остановленных контейнеров
docker rm $(docker ps -aq)
```

---

**Готово! Теперь Docker установлен и готов к работе!** 🎉

Если возникнут проблемы при установке - напишите, помогу решить!

