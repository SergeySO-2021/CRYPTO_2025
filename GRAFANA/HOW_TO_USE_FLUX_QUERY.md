# 🔍 КАК ИСПОЛЬЗОВАТЬ FLUX ЗАПРОС В GRAFANA

## 📊 Вариант 1: Проверка Data Source через создание панели (Рекомендуется)

### Шаг 1: Откройте Grafana

1. Перейдите: **http://localhost:3001**
2. Войдите:
   - Логин: `admin`
   - Пароль: `admin123`

### Шаг 2: Создайте новый Dashboard

1. В левом меню найдите **Dashboards** (иконка с графиками)
2. Нажмите на **Dashboards**
3. В выпадающем меню выберите **New**
4. Выберите **New Dashboard**

### Шаг 3: Добавьте визуализацию

1. В открывшемся дашборде нажмите кнопку **Add visualization** (синяя кнопка в центре)
   
   **Или:**
   - Нажмите **Add** (кнопка сверху)
   - Выберите **Visualization**

### Шаг 4: Выберите Data Source

1. В центре экрана появится панель редактирования панели
2. В разделе **Query** (верхняя часть панели) найдите выпадающий список **Data source**
3. Выберите **InfluxDB** из списка

### Шаг 5: Переключитесь на Flux редактор

1. В области запросов найдите переключатель или вкладки:
   - **Query Builder** или **Flux**
2. Переключитесь на **Flux** (или нажмите на вкладку **Flux**)

### Шаг 6: Вставьте Flux запрос

1. Вы увидите текстовое поле с редактором кода
2. Удалите весь текст в поле (если там есть пример)
3. Вставьте следующий Flux запрос:

```flux
from(bucket: "binance_data")
  |> range(start: -365d)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["_field"] == "close")
```

**⚠️ ВАЖНО:** Используем `-365d` (год), так как последние данные от 2025-10-27. `-1h` не вернет результатов!

**Полный запрос с выбором поля "close" (цена закрытия):**

```flux
from(bucket: "binance_data")
  |> range(start: -365d)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "close")
```

### Шаг 7: Запустите запрос

1. После вставки запроса Grafana автоматически попытается его выполнить
2. Подождите несколько секунд
3. Если запрос успешен, вы увидите:
   - ✅ Данные на графике (если есть данные за последний час)
   - ✅ Таблицу с результатами внизу
   - ✅ Нет сообщений об ошибках

### Шаг 8: Проверка результата

**Если все работает правильно:**
- Внизу панели появятся данные или график
- Никаких красных сообщений об ошибках

**Если есть ошибки:**
- Проверьте, что Data Source настроен (Configuration → Data Sources → InfluxDB → Save & Test)
- Убедитесь, что данные загружены в InfluxDB (за период последнего часа)

---

## 🔧 Вариант 2: Проверка через Data Source страницу

### Шаг 1: Откройте Data Source настройки

1. В левом меню: **⚙️ Configuration → Data Sources**
2. Нажмите на **InfluxDB**

### Шаг 2: Найдите Query Inspector

1. Прокрутите страницу вниз
2. Найдите раздел **Query Inspector** (может быть внизу страницы или в отдельной вкладке)

### Шаг 3: Тестирование запроса

1. В Query Inspector найдите поле для ввода Flux запроса
2. Вставьте запрос:
```flux
from(bucket: "binance_data")
  |> range(start: -365d)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
```

**⚠️ Используем `-365d`, так как последние данные от 2025-10-27!**

3. Нажмите кнопку **Run** или **Execute Query**

---

## 📊 Вариант 3: Создание полноценного графика (Продвинутый)

### Полный запрос для графика цены BTC:

```flux
from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "close")
  |> aggregateWindow(every: 15m, fn: last, createEmpty: false)
```

**Где использовать:**
1. Создайте Dashboard → Add visualization
2. Выберите Data Source: **InfluxDB**
3. Переключитесь на **Flux**
4. Вставьте запрос выше
5. Внизу панели настройте:
   - **Panel type:** Time series (временной ряд)
   - **Title:** "BTC Price (Close)"

---

## 📝 Объяснение запроса

Разберем запрос по частям:

```flux
from(bucket: "binance_data")
```
- Выбираем bucket (базу данных) `binance_data`

```flux
  |> range(start: -1h)
```
- ⚠️ **ВНИМАНИЕ:** Ваши данные заканчиваются 2025-10-27, поэтому `-1h` (последний час) не вернет данных!
- Используйте конкретные даты или большой диапазон:
  - `range(start: 2024-01-31T00:00:00Z, stop: 2025-10-27T23:59:59Z)` - все данные
  - `range(start: -365d)` - последний год (захватит ваши данные)
  - `range(start: v.timeRangeStart, stop: v.timeRangeStop)` - диапазон выбранный на дашборде

```flux
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
```
- Фильтруем только записи с measurement `ohlcv` (наши OHLCV данные)

```flux
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
```
- Фильтруем только данные для торговой пары BTCUSDT

```flux
  |> filter(fn: (r) => r["_field"] == "close")
```
- Выбираем поле "close" (цена закрытия)
- Можно заменить на: `"open"`, `"high"`, `"low"`, `"volume"`

---

## 🎯 Полезные варианты запросов

### Запрос для объема (Volume):

```flux
from(bucket: "binance_data")
  |> range(start: -365d)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "volume")
  |> aggregateWindow(every: 15m, fn: sum, createEmpty: false)
```

**⚠️ Используем `-365d` вместо `-24h`, так как последние данные от 2025-10-27**

### Запрос для всех данных за период:

```flux
from(bucket: "binance_data")
  |> range(start: -365d)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
```

**Или с конкретными датами (все ваши данные):**
```flux
from(bucket: "binance_data")
  |> range(start: 2024-01-31T00:00:00Z, stop: 2025-10-27T23:59:59Z)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
```

### Запрос для диапазона дат на дашборде:

```flux
from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "close")
  |> aggregateWindow(every: 15m, fn: last, createEmpty: false)
```

---

## ⚠️ Частые ошибки и решения

### Ошибка: "bucket not found"

**Причина:** Bucket не найден или неправильное название

**Решение:**
```flux
// Проверьте название bucket (должно быть "binance_data")
from(bucket: "binance_data")  // ✅ Правильно
from(bucket: "Binance_Data")  // ❌ Неправильно (регистр важен!)
```

### Ошибка: "no data returned"

**Причина:** Нет данных за выбранный период (ваши данные: 2024-01-31 до 2025-10-27)

**Решение:**
```flux
// Используйте правильный диапазон для ваших данных:
|> range(start: -365d)    // Захватит весь год (рекомендуется)
// Или конкретные даты ваших данных:
|> range(start: 2024-01-31T00:00:00Z, stop: 2025-10-27T23:59:59Z)
// Или используйте переменные дашборда:
|> range(start: v.timeRangeStart, stop: v.timeRangeStop)
```

### Ошибка: "field not found"

**Причина:** Неправильное название поля

**Решение:**
```flux
// Проверьте доступные поля (должно быть: open, high, low, close, volume)
|> filter(fn: (r) => r["_field"] == "close")  // ✅ Правильно
|> filter(fn: (r) => r["_field"] == "Close")  // ❌ Неправильно (регистр важен!)
```

---

## 🎨 Визуализация результата

После выполнения запроса:

1. **График будет показан автоматически** (если есть данные)
2. **Настройте тип панели:**
   - Time series - для временных рядов
   - Bar chart - для столбчатой диаграммы
   - Stat - для одной метрики

3. **Настройте отображение:**
   - Title (заголовок)
   - Unit (единицы измерения: $ для цены, BTC для объема)
   - Colors (цвета)

---

**Готово! Теперь вы знаете, как использовать Flux запросы в Grafana!** 🎉

