# 🚀 БЫСТРЫЙ СТАРТ DOCKER ДЛЯ GRAFANA

## ✅ ХОРОШИЕ НОВОСТИ!

Docker уже установлен на вашем компьютере!
- ✅ Docker version 28.5.1
- ✅ Docker Compose version 2.40.2

## 📋 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС

### ШАГ 1: Запустить Docker Desktop

1. **Найдите Docker Desktop** в меню Пуск или на рабочем столе
   - Или найдите: `Docker Desktop` в поиске Windows

2. **Запустите приложение**

3. **Дождитесь полного запуска:**
   - В системном трее (правый нижний угол) появится иконка Docker 🐳
   - Иконка должна стать **зеленой/синей** (не красной)
   - Может занять 1-2 минуты при первом запуске

4. **Откройте Docker Desktop:**
   - Должно быть написано: **"Docker Desktop is running"**
   - Если есть уведомление об обновлении - можете отложить его

### ШАГ 2: Проверка работы

Откройте PowerShell и выполните:

```powershell
docker ps
```

Если команда выполнилась **без ошибок** (даже если список пустой) - Docker работает! ✅

### ШАГ 3: Запуск Grafana + InfluxDB

Теперь можно запустить Grafana:

```powershell
cd C:\Users\XE\Desktop\CRYPTO_2025\GRAFANA\grafana
docker-compose up -d
```

Эта команда:
- Скачает образы InfluxDB и Grafana (если еще не скачаны)
- Запустит их в фоновом режиме
- Подключит порты: 8086 (InfluxDB) и 3000 (Grafana)

### ШАГ 4: Проверка запуска

```powershell
docker ps
```

Должны увидеть 2 контейнера:
- `influxdb`
- `grafana`

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: "Cannot connect to Docker daemon"

**Решение:** Docker Desktop не запущен
1. Запустите Docker Desktop из меню Пуск
2. Дождитесь, пока иконка станет зеленой
3. Повторите команду

### Проблема: "Port already in use"

**Решение:** Порты 8086 или 3000 заняты
1. Проверьте, что порты свободны:
   ```powershell
   netstat -ano | findstr :8086
   netstat -ano | findstr :3000
   ```
2. Если порты заняты, остановите процессы или измените порты в `docker-compose.yml`

### Проблема: Docker Desktop не запускается

**Решение:**
1. Перезагрузите компьютер
2. Запустите Docker Desktop от имени администратора
3. Если не помогает - см. `DOCKER_INSTALLATION_GUIDE.md`

---

## ✅ КОМАНДЫ ДЛЯ УПРАВЛЕНИЯ

```powershell
# Запустить Grafana + InfluxDB
cd GRAFANA/grafana
docker-compose up -d

# Остановить контейнеры
docker-compose down

# Просмотр логов
docker-compose logs -f

# Просмотр только логов Grafana
docker logs grafana

# Просмотр только логов InfluxDB
docker logs influxdb

# Перезапустить контейнеры
docker-compose restart

# Остановить все контейнеры
docker stop $(docker ps -aq)
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ ЗАПУСКА

1. ✅ Docker Desktop запущен
2. ✅ Контейнеры запущены (`docker ps`)
3. ✅ Откройте Grafana: http://localhost:3000
4. ✅ Откройте InfluxDB: http://localhost:8086
5. ✅ Следуйте инструкциям из `QUICK_START_15M.md`

---

**Готово! Теперь можно начинать работу с Grafana!** 🎉









