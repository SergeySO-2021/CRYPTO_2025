# 📊 Инструкция по использованию BTC Price & Volume Dashboard

## 🎯 Описание

Этот дашборд показывает:
- **График цены BTC** (цена закрытия 15-минутных свечей)
- **Max и Min значения** при наведении на график
- **Рыночные объемы покупок** (зеленым цветом, эффект "пламени")
- **Рыночные объемы продаж** (красным цветом, эффект "пламени")

## 📋 Шаги для запуска

### 1. Сохранение объединенных данных в CSV

Объединенный CSV файл уже создан:
```
C:\Users\XE\Desktop\CRYPTO_2025\binance_data_collector\BTCUSDT_15m_COMBINED.csv
```

### 2. Загрузка данных в InfluxDB

Запустите скрипт для загрузки данных:

```bash
cd C:\Users\XE\Desktop\CRYPTO_2025
python load_combined_to_influxdb.py
```

Или вручную:
```bash
python load_combined_to_influxdb.py --csv-file "C:\Users\XE\Desktop\CRYPTO_2025\binance_data_collector\BTCUSDT_15m_COMBINED.csv"
```

**Параметры по умолчанию:**
- InfluxDB URL: `http://localhost:8086`
- Token: `my-super-secret-admin-token`
- Org: `crypto`
- Bucket: `binance_data`

### 3. Импорт дашборда в Grafana

#### Вариант 1: Через UI Grafana

1. Откройте Grafana: **http://localhost:3001**
2. Войдите (admin/admin123)
3. Перейдите: **Dashboards → Import**
4. Нажмите **Upload JSON file**
5. Выберите файл: `GRAFANA/grafana/dashboards/btc_price_volume_dashboard.json`
6. Нажмите **Load**

#### Вариант 2: Копирование JSON

1. Откройте Grafana: **http://localhost:3001**
2. Перейдите: **Dashboards → New → Import**
3. Скопируйте содержимое файла `btc_price_volume_dashboard.json`
4. Вставьте в поле **Import via panel json**
5. Нажмите **Load**

### 4. Проверка данных

После импорта дашборда:

1. Выберите временной диапазон (например, последние 7 дней)
2. Убедитесь, что данные отображаются:
   - График цены должен показывать линию
   - Объемы должны отображаться в виде столбцов (зеленые и красные)

## 📊 Структура дашборда

### Панель 1: BTC Price (Close) - 15m
- **Тип**: Time series
- **Отображает**: Цену закрытия 15-минутных свечей
- **При наведении**: Показывает Max и Min значения
- **Цвет**: Синий

### Панель 2: Market Buy Volume (Green Flame)
- **Тип**: Time series (bars)
- **Отображает**: Объемы покупок (trades_buy_volume)
- **Стиль**: Столбцы с градиентом (эффект "пламени")
- **Цвет**: Зеленый

### Панель 3: Market Sell Volume (Red Flame)
- **Тип**: Time series (bars)
- **Отображает**: Объемы продаж (trades_sell_volume)
- **Стиль**: Столбцы с градиентом (эффект "пламени")
- **Цвет**: Красный

### Панель 4: Market Buy/Sell Volume Comparison
- **Тип**: Time series
- **Отображает**: Сравнение объемов покупок и продаж
- **Цвета**: Зеленый (покупки), Красный (продажи)

## 🔍 Запросы Flux

### График цены:
```flux
from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "btc_combined")
  |> filter(fn: (r) => r["_field"] == "close")
  |> aggregateWindow(every: 15m, fn: last, createEmpty: false)
```

### Объемы покупок:
```flux
from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "btc_combined")
  |> filter(fn: (r) => r["_field"] == "trades_buy_volume")
  |> aggregateWindow(every: 15m, fn: sum, createEmpty: false)
```

### Объемы продаж:
```flux
from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "btc_combined")
  |> filter(fn: (r) => r["_field"] == "trades_sell_volume")
  |> aggregateWindow(every: 15m, fn: sum, createEmpty: false)
```

## ⚠️ Устранение проблем

### Нет данных на графике

1. **Проверьте подключение к InfluxDB:**
   - Configuration → Data Sources → InfluxDB → Save & Test
   - Должно быть: "Data source is working"

2. **Проверьте наличие данных:**
   - В Query Inspector проверьте запрос
   - Убедитесь, что measurement `btc_combined` существует
   - Проверьте временной диапазон (данные с 2024-01-01)

3. **Проверьте bucket:**
   - Убедитесь, что bucket `binance_data` существует
   - Проверьте права доступа

### График не показывает Max/Min при наведении

1. Откройте настройки панели (Edit)
2. Проверьте, что в Targets есть запросы для `high` и `low`
3. Убедитесь, что `hide` не установлен в `true` для этих запросов
4. В Tooltip settings должен быть режим `multi`

### Объемы не отображаются как "пламя"

1. Откройте настройки панели (Edit)
2. В Visualization settings:
   - Draw style: `Bars`
   - Fill opacity: `80`
   - Gradient mode: `Opacity`
3. Цвета должны быть: зеленый для покупок, красный для продаж

## 📁 Файлы

- **CSV данные**: `binance_data_collector/BTCUSDT_15m_COMBINED.csv`
- **Скрипт загрузки**: `load_combined_to_influxdb.py`
- **Дашборд**: `GRAFANA/grafana/dashboards/btc_price_volume_dashboard.json`

## 🎨 Кастомизация

### Изменение цветов

1. Откройте панель в режиме редактирования
2. Перейдите в **Field overrides**
3. Выберите поле (например, `trades_buy_volume`)
4. Измените цвет в настройках

### Изменение временного диапазона по умолчанию

В JSON файле дашборда измените:
```json
"time": {
  "from": "now-7d",  // Измените на нужный период
  "to": "now"
}
```

## ✅ Готово!

Теперь у вас есть красивый дашборд с графиком цены и объемов в виде "пламени"! 🔥

