# 🔧 УСТРАНЕНИЕ ПРОБЛЕМ: Дашборд не показывает данные

## ❌ Проблема: "Ничего не происходит" после вставки запроса

Если вы вставили Flux запрос, но ничего не отображается, проверьте следующие пункты:

---

## ✅ ШАГ 1: Проверка Data Source

### 1.1 Убедитесь, что Data Source настроен:

1. Откройте: **Configuration → Data Sources**
2. Найдите **InfluxDB** в списке
3. Нажмите на **InfluxDB**
4. Нажмите **Save & Test** внизу страницы
5. **Должна появиться зеленая галочка** ✅ "Data source is working"

**Если галочки нет:**
- Проверьте, что InfluxDB запущен: `docker ps | findstr influxdb`
- Проверьте настройки подключения (см. `GRAFANA_DATASOURCE_SETUP.md`)

---

## ✅ ШАГ 2: Правильная вставка запроса

### 2.1 Проверьте формат запроса

**❌ НЕПРАВИЛЬНО:** Копирование с символами ```flux
```flux
from(bucket: "binance_data")
...
```

**✅ ПРАВИЛЬНО:** Только текст запроса
```
from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "close")
  |> aggregateWindow(every: 15m, fn: last, createEmpty: false)
```

### 2.2 Где именно вставлять:

1. **Создайте Dashboard:** Dashboards → New → New Dashboard
2. **Добавьте панель:** Add visualization (или Add → Visualization)
3. **Выберите Data Source:** В верхней части панели, выпадающий список - выберите **InfluxDB**
4. **Переключитесь на Flux:**
   - Найдите переключатель **Query Builder / Flux** или вкладки
   - Переключитесь на **Flux**
5. **Вставьте запрос** в большое текстовое поле (обычно в центре экрана)

---

## ✅ ШАГ 3: Проверка диапазона времени

### 3.1 Настройте временной диапазон на дашборде:

**⚠️ ВАЖНО:** `v.timeRangeStart` и `v.timeRangeStop` работают только если выбран временной диапазон на дашборде!

1. В правом верхнем углу дашборда найдите **селектор времени** (обычно показывает "Last 6 hours")
2. Нажмите на него
3. Выберите период, который включает ваши данные:
   - **Last 7 days** - минимальный период
   - **Last 30 days** - лучше
   - **Last 12 months** - оптимально (захватит все данные)
   - **Custom range:** 2024-01-31 до 2025-10-27

### 3.2 Альтернатива: Используйте конкретные даты

Если `v.timeRangeStart` не работает, используйте конкретный период:

```flux
from(bucket: "binance_data")
  |> range(start: 2024-01-31T00:00:00Z, stop: 2025-10-27T23:59:59Z)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "close")
  |> aggregateWindow(every: 15m, fn: last, createEmpty: false)
```

---

## ✅ ШАГ 4: Проверка видимости результата

### 4.1 Найдите область с результатами:

После вставки запроса, результаты могут быть:
- **Внизу панели** - таблица с данными
- **В центре** - график (если есть данные)
- **В Query Inspector** - детальная информация

### 4.2 Откройте Query Inspector:

1. В панели редактирования найдите кнопку **Query Inspector** (обычно справа вверху)
2. Или нажмите на иконку **Inspect** (лупа)
3. Откройте вкладку **Query** или **Data**
4. Проверьте:
   - Есть ли данные в таблице?
   - Есть ли ошибки в логах?

---

## ✅ ШАГ 5: Проверка настройки панели

### 5.1 Тип панели:

1. В правой части экрана найдите настройки панели
2. Убедитесь, что выбран тип: **Time series** (для временных рядов)

### 5.2 Transformation (если нужно):

Если данные есть, но график не отображается:
1. Проверьте вкладку **Transform** (слева внизу)
2. Убедитесь, что выбрано поле для отображения

---

## ✅ ШАГ 6: Упрощенный тестовый запрос

Попробуйте сначала простой запрос для проверки:

```flux
from(bucket: "binance_data")
  |> range(start: -365d)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> limit(n: 100)
```

Этот запрос вернет первые 100 записей. Если он работает, значит проблема в диапазоне времени.

---

## 🔍 ШАГ 7: Диагностика через InfluxDB UI

Проверьте данные напрямую:

1. Откройте InfluxDB UI: **http://localhost:8086**
2. Войдите: `admin` / `admin123`
3. Перейдите в **Data Explorer**
4. Выберите:
   - **Bucket:** `binance_data`
   - **Measurement:** `ohlcv`
   - **Field:** `close`
5. Выполните запрос - должны увидеть данные

**Если данных нет в InfluxDB UI:**
- Данные не загружены в InfluxDB
- Нужно запустить скрипт загрузки (см. `QUICK_START_15M.md`)

---

## 🛠️ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: "Query editor not initialized"

**Решение:**
1. Убедитесь, что Data Source выбран (вверху панели)
2. Перезагрузите страницу Grafana
3. Пересоздайте панель

### Проблема 2: "No data"

**Решение:**
1. Проверьте временной диапазон (ШАГ 3)
2. Убедитесь, что данные есть в InfluxDB (ШАГ 7)
3. Попробуйте упрощенный запрос (ШАГ 6)

### Проблема 3: "Query timeout"

**Решение:**
1. Уменьшите диапазон времени
2. Добавьте `limit(n: 10000)` в конец запроса:
```flux
...
  |> limit(n: 10000)
```

### Проблема 4: График пустой, но данные есть в Query Inspector

**Решение:**
1. Проверьте тип панели (должен быть **Time series**)
2. Убедитесь, что выбран правильный field для отображения
3. Проверьте настройки оси Y (может быть неправильный масштаб)

---

## ✅ ПОШАГОВЫЙ ЧЕКЛИСТ

Отметьте каждое действие:

- [ ] Data Source настроен и работает (зеленая галочка)
- [ ] Запрос вставлен БЕЗ символов ```flux и ```
- [ ] Выбран правильный Data Source (InfluxDB)
- [ ] Переключено на Flux (не Query Builder)
- [ ] Настроен временной диапазон на дашборде (Last 12 months или Custom)
- [ ] Запрос сохранен (нажмите кнопку Apply или сохраните панель)
- [ ] Проверен Query Inspector на наличие данных
- [ ] Тип панели установлен как Time series

---

## 📋 ГОТОВЫЙ РАБОЧИЙ ЗАПРОС (Скопируйте полностью):

```
from(bucket: "binance_data")
  |> range(start: 2024-01-31T00:00:00Z, stop: 2025-10-27T23:59:59Z)
  |> filter(fn: (r) => r["_measurement"] == "ohlcv")
  |> filter(fn: (r) => r["symbol"] == "BTCUSDT")
  |> filter(fn: (r) => r["timeframe"] == "15m")
  |> filter(fn: (r) => r["_field"] == "close")
  |> aggregateWindow(every: 15m, fn: last, createEmpty: false)
```

**Этот запрос должен точно работать**, так как использует конкретные даты ваших данных.

---

## 🆘 Если ничего не помогает:

1. **Проверьте логи Grafana:**
   ```bash
   docker logs grafana --tail 50
   ```

2. **Проверьте логи InfluxDB:**
   ```bash
   docker logs influxdb --tail 50
   ```

3. **Перезапустите контейнеры:**
   ```bash
   cd GRAFANA/grafana
   docker-compose restart
   ```

4. **Создайте новый дашборд заново:**
   - Удалите текущий
   - Создайте новый
   - Повторите шаги

---

**После выполнения всех проверок, график должен появиться!** 🎉

