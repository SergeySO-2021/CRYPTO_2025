# 📊 Обновления дашборда BTC Price & Volume

## ✅ Что изменено

### 1. Объемы в долларах (USD)
- Рыночные объемы покупок и продаж теперь рассчитываются в долларах
- Формула: `trades_buy_volume * close` (объем в BTC × цена = объем в USD)
- Формат отображения: `currencyUSD` (автоматически показывает в тысячах/миллионах)

### 2. Шкала справа
- Y-axis (шкала значений) перемещена справа для всех панелей объемов
- Настройка: `axisPlacement: "right"` в fieldConfig

### 3. Отображение в тысячах
- Unit: `currencyUSD` - автоматически форматирует большие числа
- Decimals: 1 - показывает одну десятичную для точности
- Пример отображения: `1.5K USD`, `2.3M USD` (в тысячах/миллионах)

### 4. Tooltip (при наведении)
- При наведении на "пламя" показываются значения в долларах
- Формат: тысячах/миллионах (например: "1,234.5K USD" или "2.3M USD")

## 📋 Структура панелей

1. **BTC Price (Close) - 15m**
   - График цены закрытия
   - При наведении: показывает Max и Min значения

2. **Market Buy Volume (Green Flame) - USD**
   - Объемы покупок в долларах
   - Зеленый цвет, эффект "пламени"
   - Шкала справа

3. **Market Sell Volume (Red Flame) - USD**
   - Объемы продаж в долларах
   - Красный цвет, эффект "пламени"
   - Шкала справа

4. **Market Buy/Sell Volume Comparison - USD**
   - Сравнение объемов покупок и продаж
   - Оба в долларах
   - Шкала справа

## 🔍 Flux запросы

### Объемы покупок в USD:
```flux
data = from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "btc_combined")
  |> pivot(rowKey: ["_time"], columnKey: ["_field"], valueColumn: "_value")
  |> map(fn: (r) => ({ r with _value: r.trades_buy_volume * r.close, _field: "buy_volume_usd" }))
```

### Объемы продаж в USD:
```flux
data = from(bucket: "binance_data")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "btc_combined")
  |> pivot(rowKey: ["_time"], columnKey: ["_field"], valueColumn: "_value")
  |> map(fn: (r) => ({ r with _value: r.trades_sell_volume * r.close, _field: "sell_volume_usd" }))
```

## 📝 Примечания

- **Формат чисел**: Grafana автоматически форматирует большие числа в `currencyUSD`:
  - Меньше 1000: `123.4 USD`
  - Тысячи: `1.5K USD` (1,500 USD)
  - Миллионы: `2.3M USD` (2,300,000 USD)
  
- **Шкала справа**: Все панели объемов используют правую Y-axis для лучшей читаемости

- **Tooltip**: При наведении показывается точное значение в долларах с форматированием

## 🎨 Визуализация

- **Эффект "пламени"**: Достигается через:
  - `drawStyle: "bars"` - столбцы
  - `gradientMode: "opacity"` - градиент прозрачности
  - `fillOpacity: 80` - прозрачность заливки
  - Цвета: зеленый для покупок, красный для продаж

## ✅ Готово к использованию!

Дашборд готов к импорту в Grafana. Все объемы отображаются в долларах с шкалой справа и форматированием в тысячах/миллионах.

