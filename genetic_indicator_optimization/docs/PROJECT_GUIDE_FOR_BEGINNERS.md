# 📖 Методичка по проекту: Генетическая оптимизация торговых индикаторов

**Для кого:** для тех, кто хочет понять проект с нуля  
**Уровень:** от простого объяснения к техническим деталям  
**Дата:** 20 ноября 2025

---

## 🎯 Часть 1: Что это за проект? (Простыми словами)

### Представьте себе...

Вы хотите заработать на бирже, покупая и продавая биткоин. Но проблема в том, что нужно решать: **когда покупать?** и **когда продавать?** 

Есть специальные "индикаторы" — это как приборы, которые показывают, что происходит на рынке. Например:
- **RSI** — показывает, перекуплен ли рынок (как термометр показывает температуру)
- **MACD** — показывает, куда движется тренд (как компас показывает направление)
- **Bollinger Bands** — показывают, насколько волатилен рынок (как барометр показывает давление)

Но у каждого индикатора есть **настройки** (параметры). Например, RSI можно считать за 10 дней, за 15 дней или за 20 дней. И от этого результат будет разный!

### В чём проблема?

Если попробовать все возможные комбинации настроек вручную, это займёт **тысячи лет**. Например:
- RSI период: 10, 11, 12... 25 (16 вариантов)
- Stop Loss: 0.5%, 0.6%, 0.7%... 2.0% (16 вариантов)
- Take Profit: 1.0%, 1.1%... 5.0% (41 вариант)
- И ещё 20+ параметров...

**Итого:** 16 × 16 × 41 × ... = **миллиарды комбинаций!**

### Наше решение

Мы используем **генетический алгоритм** — это как "эволюция" в компьютере. Алгоритм сам пробует разные комбинации параметров, находит лучшие и "скрещивает" их, чтобы получить ещё лучшие. Это как селекция растений: берём лучшие сорта и создаём из них ещё лучшие.

**Результат:** вместо тысяч лет поиска, алгоритм находит хорошие настройки за несколько часов!

---

## 🎯 Часть 2: Какие задачи мы ставили?

### Главная цель

Создать **автоматическую систему**, которая:
1. Сама находит лучшие настройки индикаторов
2. Проверяет, что стратегия работает не только на старых данных, но и на новых
3. Учитывает реальные условия торговли (комиссии, проскальзывание)

### Конкретные задачи

#### Задача 1: Построить пайплайн (трубопровод данных)
**Что это значит?** Создать систему, которая:
- Загружает данные о ценах биткоина
- Вычисляет индикаторы (RSI, MACD и т.д.)
- Генерирует сигналы "покупать" или "продавать"
- Симулирует торговлю (бэктест) с учётом комиссий
- Оценивает, насколько хорошо работает стратегия

**Почему это важно?** Без этого пайплайна мы не можем проверить, работают ли наши настройки.

#### Задача 2: Реализовать генетический алгоритм
**Что это значит?** Создать алгоритм, который:
- Генерирует случайные наборы параметров (как "особи" в популяции)
- Оценивает каждую "особь" (насколько хорошо она торгует)
- Отбирает лучших (селекция)
- "Скрещивает" лучших (кроссовер) — берёт часть параметров от одного, часть от другого
- Мутирует (случайно меняет некоторые параметры)
- Повторяет процесс много раз (поколения)

**Почему это важно?** Это позволяет автоматически искать лучшие настройки.

#### Задача 3: Избежать переобучения
**Что это значит?** Переобучение — это когда стратегия отлично работает на старых данных, но проваливается на новых. Это как ученик, который выучил все ответы наизусть, но не понимает материал.

**Почему это важно?** Если стратегия переобучена, она не будет работать в реальной торговле.

#### Задача 4: Учесть реальные условия
**Что это значит?** В реальной торговле:
- Нужно платить комиссии (0.02–0.07% за сделку)
- Цена может "проскальзывать" (вы хотели купить по 100$, а купили по 100.05$)
- Нужно учитывать риски (стоп-лоссы, тейк-профиты)

**Почему это важно?** Если не учесть эти факторы, стратегия будет показывать хорошие результаты на бумаге, но плохие в реальности.

---

## 🎯 Часть 3: Как мы решали задачи? (Методы простым языком)

### Метод 1: Разделение данных на три части

**Простое объяснение:** Представьте, что вы готовитесь к экзамену:
- **Train (обучение)** — это учебник, по которому вы учитесь (70% данных)
- **Val (валидация)** — это пробный экзамен, чтобы проверить, как вы усвоили материал (15% данных)
- **Test (тест)** — это настоящий экзамен, который показывает, готовы ли вы к реальности (15% данных)

**Зачем это нужно?** Чтобы убедиться, что стратегия работает не только на данных, по которым она обучалась, но и на новых данных.

**Как мы это делали:**
```python
# Упрощённый пример
train_data = данные[0:70%]      # Первые 70% данных
val_data = данные[70%:85%]       # Следующие 15%
test_data = данные[85%:100%]     # Последние 15%
```

### Метод 2: Генетический алгоритм

**Простое объяснение:** Это как эволюция в природе:
1. **Популяция** — группа из 100 "особей" (наборов параметров)
2. **Оценка** — каждая особь торгует, и мы смотрим, сколько она заработала
3. **Селекция** — выбираем 20 лучших (как в спорте отбирают чемпионов)
4. **Кроссовер** — "скрещиваем" лучших (берём RSI от одного, Stop Loss от другого)
5. **Мутация** — случайно меняем некоторые параметры (как случайные мутации в природе)
6. **Повторение** — делаем это 100 раз (100 поколений)

**Результат:** Через 100 поколений получаем очень хорошие параметры!

**Как это выглядит в коде:**
```python
# Упрощённый пример
популяция = создать_100_случайных_наборов_параметров()

for поколение in range(100):
    результаты = []
    for особь in популяция:
        прибыль = протестировать_стратегию(особь)
        результаты.append(прибыль)
    
    лучшие = выбрать_топ_20(результаты)
    новые_особи = скрестить_и_мутировать(лучшие)
    популяция = новые_особи
```

### Метод 3: Fitness-функция (функция оценки)

**Простое объяснение:** Это как система оценок в школе. Мы оцениваем стратегию по нескольким критериям:
- **Прибыльность** (35% оценки) — сколько заработали
- **Стабильность** (25% оценки) — насколько стабильна прибыль (Sharpe Ratio)
- **Риск** (20% оценки) — какая максимальная просадка была
- **Точность** (10% оценки) — сколько сделок было прибыльных (Win Rate)
- **Эффективность** (10% оценки) — соотношение прибыли к убыткам (Profit Factor)

**Штрафы:** Если стратегия плохая (мало сделок, большие убытки), мы снижаем оценку.

**Пример:**
```
Если стратегия:
- Заработала +7% на валидации → +2.45 балла (35% от 7)
- Sharpe Ratio = 42 → +10.5 балла (25% от 42)
- Max Drawdown = 2.8% → +7.14 балла (20% от (100-2.8))
- Win Rate = 50% → +5 баллов (10% от 50)
- Profit Factor = 2.1 → +2.1 балла (10% от 2.1)
- Мало сделок (штраф) → -5 баллов

Итого: Fitness = 2.45 + 10.5 + 7.14 + 5 + 2.1 - 5 = 22.19
```

### Метод 4: Параллелизация

**Простое объяснение:** Вместо того чтобы проверять стратегии по очереди (одну за другой), мы проверяем несколько одновременно, используя несколько ядер процессора.

**Аналогия:** Вместо того чтобы один повар готовил 100 блюд по очереди, мы нанимаем 4 поваров, и каждый готовит 25 блюд одновременно.

**Результат:** Вместо 4 часов работы, алгоритм работает 1 час!

---

## 🎯 Часть 4: С какими проблемами мы столкнулись?

### Проблема 1: Переобучение (Overfitting)

**Что это?** Стратегия отлично работает на данных, по которым обучалась, но проваливается на новых данных.

**Пример из жизни:** Ученик выучил все задачи из учебника наизусть, но на экзамене получает новые задачи и не может их решить.

**Что происходило у нас:**
- На Train (обучение): +8.86% прибыли ✅
- На Val (валидация): +8.80% прибыли ✅
- На Test (тест): **-4.08% убытка** ❌

**Почему это случилось?** Алгоритм "запомнил" особенности старых данных, но не научился общим закономерностям.

### Проблема 2: Fitness = Infinity (бесконечность)

**Что это?** В некоторых случаях наша функция оценки возвращала "бесконечность" вместо числа.

**Почему это происходило?** 
- Если на валидации была только одна сделка, и она была прибыльной, то Profit Factor = "бесконечность" (деление на ноль: прибыль / 0 убытков)
- Если волатильность была нулевой, то Sharpe Ratio = "бесконечность"

**Что это означало?** Алгоритм не мог сравнить стратегии, потому что все "плохие" стратегии получали бесконечную оценку.

**Пример:**
```
Стратегия A: Fitness = 200 (хорошая)
Стратегия B: Fitness = Infinity (плохая, но из-за ошибки получила бесконечность)
Алгоритм выбирает B, потому что Infinity > 200 ❌
```

### Проблема 3: Слишком мало сделок

**Что это?** Некоторые стратегии делали очень мало сделок (например, 2–3 сделки за весь период).

**Почему это плохо?** 
- Мало данных для оценки (2 сделки — это не статистика)
- Случайность: если из 2 сделок обе прибыльные, это может быть просто удача, а не хорошая стратегия

**Что происходило:**
```
Стратегия делает 2 сделки:
- Сделка 1: +100$ (прибыль)
- Сделка 2: +50$ (прибыль)
Win Rate = 100%, Profit Factor = Infinity
Но это не значит, что стратегия хорошая!
```

### Проблема 4: Нелогичные параметры

**Что это?** Алгоритм находил параметры, которые математически возможны, но логически бессмысленны.

**Пример:**
- MACD fast period = 24 (быстрая линия)
- MACD slow period = 22 (медленная линия)

**Проблема:** Быстрая линия не может быть медленнее медленной! Это как сказать, что черепаха быстрее зайца.

**Почему это происходило?** Алгоритм не знал о логических ограничениях между параметрами.

### Проблема 5: Слишком широкое временное окно

**Что это?** Алгоритм выбирал торговать почти весь день (например, 11 часов из 24).

**Почему это плохо?** 
- Это "ленивое" решение: если торговать весь день, больше шансов попасть в прибыльные моменты просто случайно
- Не показывает, что стратегия действительно хорошая, а не просто "везёт"

**Что происходило:**
```
Стратегия торгует с 03:00 до 14:00 (11 часов)
Делает 200 сделок
Прибыль +8.80% на валидации
Но на тесте: -4.08% ❌
```

---

## 🎯 Часть 5: Как мы решали проблемы?

### Решение 1: Борьба с переобучением

**Что мы сделали:**

1. **Изменили функцию оценки (Fitness):**
   - Раньше: оценивали только на Train (обучение)
   - Теперь: оцениваем на Val (валидация)
   - **Результат:** Алгоритм ищет стратегии, которые работают на новых данных

2. **Добавили штрафы за переобучение:**
   ```python
   # Упрощённый пример
   если (прибыль_на_валидации - прибыль_на_тесте) > 50%:
       штраф = -1000 баллов  # Большой штраф!
   ```

3. **Жёсткий запрет на отрицательный тест:**
   ```python
   если прибыль_на_тесте < 0:
       стратегия_отбрасывается  # Не рассматриваем вообще
   ```

**Результат:** После этих изменений (ГА №7) стратегия показывает:
- Val: +7.73%
- Test: +7.91% ✅ (почти одинаково!)

### Решение 2: Исправление Infinity

**Что мы сделали:**

1. **Создали функцию "безопасного значения":**
   ```python
   def безопасное_значение(метрика):
       если метрика == бесконечность:
           если это Profit Factor:
               вернуть 10.0  # Вместо бесконечности
           если это Sharpe Ratio:
               вернуть 50.0  # Вместо бесконечности
       вернуть метрика
   ```

2. **Проверяем все метрики перед расчётом:**
   ```python
   profit_factor = безопасное_значение(profit_factor)
   sharpe_ratio = безопасное_значение(sharpe_ratio)
   ```

3. **Добавили жёсткие ограничения:**
   ```python
   если количество_сделок < 10:
       стратегия_отбрасывается  # Не рассматриваем
   ```

**Результат:** Fitness всегда конечное число, алгоритм может правильно сравнивать стратегии.

### Решение 3: Штрафы за мало сделок

**Что мы сделали:**

1. **Аддитивные штрафы (вычитаем баллы):**
   ```python
   если сделок < 5:
       штраф = -1000 баллов
   если сделок < 10:
       штраф = -500 баллов
   если сделок < 20:
       штраф = -200 баллов
   ```

2. **Мультипликативные штрафы (умножаем на коэффициент):**
   ```python
   если сделок < 10:
       fitness = fitness * 0.1  # Уменьшаем в 10 раз!
   если сделок < 20:
       fitness = fitness * 0.3  # Уменьшаем в 3 раза
   ```

**Результат:** Алгоритм предпочитает стратегии с достаточным количеством сделок.

### Решение 4: Добавление ограничений (Constraints)

**Что мы сделали:**

1. **Создали функцию применения ограничений:**
   ```python
   def применить_ограничения(параметры):
       # MACD: fast должен быть меньше slow
       если macd_fast >= macd_slow:
           macd_fast = macd_slow - 5  # Исправляем
       
       # WOBI: сумма весов должна быть 1.0
       сумма = вес1 + вес2 + вес3 + вес4
       если сумма != 1.0:
           # Нормализуем (делим каждый вес на сумму)
           вес1 = вес1 / сумма
           вес2 = вес2 / сумма
           # ...
       
       # Long/Short: сумма множителей >= 1.6
       если long_mult + short_mult < 1.6:
           # Увеличиваем оба пропорционально
           коэффициент = 1.6 / (long_mult + short_mult)
           long_mult = long_mult * коэффициент
           short_mult = short_mult * коэффициент
       
       вернуть параметры
   ```

2. **Применяем ограничения после каждого изменения параметров:**
   - После создания новой особи
   - После скрещивания (кроссовер)
   - После мутации

**Результат:** Все параметры всегда логичны и корректны.

### Решение 5: Штраф за широкое временное окно

**Что мы сделали:**

```python
если длина_временного_окна > 8_часов:
    штраф = -50 * (длина_окна - 8)  # За каждый лишний час
```

**Результат:** Алгоритм предпочитает более узкие окна (например, 4 часа вместо 11).

---

## 🎯 Часть 6: Какие результаты мы получили?

### Итоговый результат (ГА №7, финальный)

**Конфигурация:**
- Популяция: 100 особей
- Поколений: 100
- Время работы: ~2–3 часа (благодаря параллелизации)

**Найденные параметры:**
- RSI период: 25
- Stop Loss: 0.77%
- Take Profit: 4.25%
- Временное окно: 01:00–05:00 UTC (4 часа)
- Long multiplier: 1.125
- Short multiplier: 0.905

**Метрики:**

| Срез | Прибыль | Sharpe | Max DD | Win Rate | Profit Factor | Сделок |
|------|---------|--------|--------|----------|---------------|--------|
| **Train** | +8.76% | 10.06 | 4.77% | 41.0% | 1.20 | 161 |
| **Val** | **+7.73%** | **52.97** | **2.29%** | **66.7%** | **2.59** | **33** |
| **Test** | **+7.91%** | **31.87** | **1.94%** | **48.6%** | **2.16** | **35** |

**Ключевые достижения:**
1. ✅ **Нет переобучения:** Val и Test почти одинаковы (7.73% vs 7.91%)
2. ✅ **Высокий Sharpe:** 52.97 на валидации, 31.87 на тесте (отличная стабильность)
3. ✅ **Низкий риск:** Max Drawdown всего 2.29% на валидации
4. ✅ **Хороший Profit Factor:** > 2.0 на обоих срезах (прибыль в 2 раза больше убытков)
5. ✅ **Логичные параметры:** Все ограничения соблюдены (MACD fast < slow, WOBI веса = 1.0)

### Сравнение с предыдущими запусками

| Запуск | Fitness | Val Return | Test Return | Проблема |
|--------|---------|------------|-------------|----------|
| ГА №1 | 137.96 | +1.7% | +4.2% | Переобучение (Train -32%) |
| ГА №2 | 85.92 | +4.55% | -0.6% | Нестабильность |
| ГА №3 | 200.85 | +7.36% | +4.02% | Хорошо, но Infinity проблема |
| ГА №4 | ∞ | +0.04% | -1.55% | Fitness = Infinity |
| ГА №5 | 200.53 | +7.54% | +3.96% | Хорошо, но можно лучше |
| ГА №6 | 208.07 | +8.80% | **-4.08%** | Переобучение |
| **ГА №7** | **223.11** | **+7.73%** | **+7.91%** | **✅ Лучший результат!** |

**Вывод:** ГА №7 — это лучший результат: высокая прибыльность, стабильность и отсутствие переобучения.

---

## 🎯 Часть 7: Технические детали (для тех, кто хочет глубже)

### Архитектура системы

```
┌─────────────┐
│  Данные     │  CSV файл с ценами BTCUSDT (15 минут)
│  (CSV)      │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ DataLoader  │  Загружает и разделяет на Train/Val/Test
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Indicator   │  Вычисляет RSI, MACD, Bollinger, WOBI
│ Pipeline    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Strategy    │  Генерирует сигналы "покупать"/"продавать"
│ (MVP)       │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Backtester  │  Симулирует торговлю с комиссиями
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Genetic     │  Оптимизирует параметры
│ Optimizer   │
└─────────────┘
```

### Структура данных

**Входные данные (CSV):**
- `timestamp` — время
- `open`, `high`, `low`, `close` — цены
- `volume` — объём
- `flow_buy_volume`, `flow_trade_imbalance` — потоковые данные
- `ask_3`, `bid_3`, `ratio3`, `d3` — данные стакана на глубине 3%
- `ask_5`, `bid_5`, `ratio5`, `d5` — данные стакана на глубине 5%
- `ask_8`, `bid_8`, `ratio8`, `d8` — данные стакана на глубине 8%
- `ask_60`, `bid_60`, `ratio60`, `d60` — данные стакана на глубине 60%

**Вычисляемые индикаторы:**
- `rsi_15`, `rsi_20`, ... — RSI с разными периодами
- `macd_line`, `macd_signal`, `macd_hist` — MACD
- `boll_mid`, `boll_upper`, `boll_lower`, `boll_width` — Bollinger Bands
- `wobi`, `wobi_zscore` — WOBI (индикатор стакана)

### Генетический алгоритм: детали

**1. Кодирование (Genes):**
Каждая "особь" — это словарь параметров:
```python
особь = {
    "rsi_period": 15,
    "stop_loss_pct": 0.81,
    "take_profit_pct": 1.25,
    "atr_period": 18,
    "macd_fast_period": 12,
    "macd_slow_period": 26,
    "time_window_start": 18,
    "time_window_length": 3,
    # ... и ещё 20+ параметров
}
```

**2. Селекция (Tournament Selection):**
```python
def турнирная_селекция(популяция, размер_турнира=3):
    участники = случайно_выбрать(популяция, размер_турнира)
    победитель = лучший_по_fitness(участники)
    вернуть победитель
```

**3. Кроссовер (Uniform Crossover):**
```python
def кроссовер(родитель1, родитель2, вероятность=0.75):
    потомок = {}
    for параметр in родитель1:
        if случайное_число() < вероятность:
            потомок[параметр] = родитель1[параметр]
        else:
            потомок[параметр] = родитель2[параметр]
    вернуть потомок
```

**4. Мутация:**
```python
def мутация(особь, вероятность=0.15):
    for параметр in особь:
        if случайное_число() < вероятность:
            особь[параметр] = случайное_значение_в_диапазоне(параметр)
    вернуть особь
```

**5. Адаптивная мутация:**
```python
def адаптивная_мутация(поколение, застой):
    базовая_вероятность = 0.15
    if застой > 5:  # Если долго нет улучшения
        вероятность = базовая_вероятность * (1 + застой * 0.05)
    else:
        вероятность = базовая_вероятность
    вернуть min(вероятность, 0.25)  # Максимум 25%
```

### Fitness-функция: полная формула

```python
def рассчитать_fitness(метрики_валидации, метрики_теста):
    # 1. Безопасная обработка метрик
    val_pnl = безопасное_значение(метрики_валидации["total_return"])
    val_sharpe = безопасное_значение(метрики_валидации["sharpe_ratio"])
    val_dd = безопасное_значение(метрики_валидации["max_drawdown"])
    val_wr = безопасное_значение(метрики_валидации["win_rate"])
    val_pf = безопасное_значение(метрики_валидации["profit_factor"])
    
    # 2. Базовый score (взвешенная сумма)
    базовый_score = (
        val_pnl * 0.35 +
        val_sharpe * 0.25 +
        (100 - val_dd) * 0.20 +
        val_wr * 0.10 +
        val_pf * 0.10
    )
    
    # 3. Штрафы за мало сделок
    сделок = метрики_валидации["total_trades"]
    штраф_сделки, множитель_сделки = штрафы_за_сделки(сделок)
    
    # 4. Штрафы за переобучение
    test_pnl = метрики_теста["total_return"]
    if test_pnl < 0:
        return -10000  # Дисквалификация
    
    разница_доходности = abs(val_pnl - test_pnl) / max(abs(val_pnl), 0.01)
    if разница_доходности > 0.5:  # > 50%
        штраф_переобучение = -500
    
    # 5. Штраф за широкое окно
    длина_окна = параметры["time_window_length"]
    if длина_окна > 8:
        штраф_окно = -50 * (длина_окна - 8)
    
    # 6. Итоговый fitness
    fitness = (базовый_score + штраф_сделки + штраф_переобучение + штраф_окно) * множитель_сделки
    
    # 7. Проверка на Infinity/NaN
    if isinf(fitness) or isnan(fitness):
        return -10000
    
    return fitness
```

### Параллелизация

**Технология:** `pathos.multiprocessing.ProcessingPool`

**Как это работает:**
```python
from pathos.multiprocessing import ProcessingPool

pool = ProcessingPool(nodes=4)  # 4 процесса

# Вместо последовательной оценки:
# for особь in популяция:
#     fitness = оценить(особь)

# Параллельная оценка:
результаты = pool.map(оценить, популяция)
```

**Ускорение:** В 3–4 раза быстрее (зависит от количества ядер CPU).

---

## 🎯 Часть 8: Что дальше? (Планы на будущее)

### Краткосрочные планы (2 недели)

1. **Walk-forward валидация**
   - Вместо одного Train/Val/Test, использовать несколько скользящих окон
   - Это даст более надёжную оценку стратегии

2. **Расширение индикаторов**
   - Добавить SuperTrend, VWAP
   - Протестировать новые индикаторы на стабильность

3. **Автоматизация отчётов**
   - После каждого запуска ГА автоматически генерировать отчёт
   - Сохранять графики, таблицы, лучшие сделки

### Среднесрочные планы (1–3 месяца)

1. **Улучшение стратегии**
   - Добавить больше индикаторов из Order Book
   - Оптимизировать веса индикаторов

2. **Стресс-тесты**
   - Проверить стратегию на разных рыночных условиях
   - Тестировать на разных таймфреймах

3. **Production-ready**
   - Подготовить систему к реальной торговле
   - Добавить мониторинг и алерты

---

## 🎯 Заключение

### Что мы узнали?

1. **Генетические алгоритмы работают!** Они могут автоматически находить хорошие настройки для торговых стратегий.

2. **Переобучение — главный враг.** Нужно всегда проверять стратегию на новых данных и добавлять штрафы за переобучение.

3. **Детали важны.** Комиссии, проскальзывание, ограничения — всё это нужно учитывать, иначе результаты будут нереалистичными.

4. **Итеративный процесс.** Мы не сразу получили хороший результат. Потребовалось 7 запусков, множество исправлений и улучшений.

### Главный вывод

Мы создали **работающую систему**, которая:
- ✅ Автоматически находит хорошие настройки
- ✅ Работает стабильно на новых данных
- ✅ Учитывает реальные условия торговли
- ✅ Показывает прибыльность на всех срезах данных

**Fitness 223.11, Val +7.73%, Test +7.91%** — это отличный результат для автоматической оптимизации!

---

## 📚 Дополнительные материалы

Если вы хотите узнать больше:

1. **Техническая документация:**
   - `docs/project_documentation.md` — полная техническая документация
   - `docs/project_plan.md` — план работ и roadmap

2. **Архив экспериментов:**
   - `docs/archive/GA_RUN_07_ANALYSIS.md` — детальный анализ ГА №7
   - `docs/archive/GA_RUN_05_ANALYSIS.md` — анализ ГА №5
   - `docs/archive/IMPLEMENTATION_FITNESS_INFINITY_FIX.md` — как мы исправляли Infinity

3. **Код:**
   - `src/core/genetic_optimizer.py` — реализация генетического алгоритма
   - `src/strategies/mvp_strategy.py` — торговая стратегия
   - `scripts/run_ga_search.py` — скрипт для запуска ГА

---

**Удачи в изучении проекта!** 🚀

Если у вас есть вопросы, смотрите основную документацию или изучайте код.

