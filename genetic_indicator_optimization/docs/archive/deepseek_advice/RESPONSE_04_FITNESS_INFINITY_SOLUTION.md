# 🤖 ОТВЕТ DEEPSEEK: Решение проблемы fitness=Infinity и переобучения

**Дата:** 18 ноября 2025  
**Статус:** Получен и проанализирован

---

## 🎯 АНАЛИЗ ПРОБЛЕМЫ

**Основная причина:** ГА нашёл "хитрое" решение, которое минимизирует количество сделок на валидации, создавая иллюзию идеальных метрик.

**Ключевые проблемы:**
1. `profit_factor = Infinity` при 1 прибыльной сделке
2. Мультипликативные штрафы не работают с Infinity
3. Диапазоны Long/Short параметров слишком широкие
4. Недостаточно жёсткие ограничения на минимальное количество сделок

---

## 🛠️ КОНКРЕТНЫЕ РЕШЕНИЯ

### 1. Обработка бесконечных метрик

**Проблема:** `profit_factor = Infinity` или `sharpe_ratio = Infinity` приводят к бесконечному fitness.

**Решение:**

```python
def _safe_metric_value(self, value, metric_name, default=0.0):
    """Безопасное вычисление метрик с обработкой edge cases"""
    if np.isinf(value) or np.isnan(value):
        # Для profit_factor: Infinity -> ограничиваем сверху
        if metric_name == "profit_factor":
            return 10.0  # Максимальное разумное значение
        # Для sharpe: Infinity -> высокое, но конечное значение
        elif metric_name == "sharpe_ratio":
            return 50.0 if value > 0 else -50.0
        else:
            return default
    return value
```

**Ключевые моменты:**
- `profit_factor = Infinity` → ограничиваем до 10.0
- `sharpe_ratio = Infinity` → ограничиваем до 50.0 (или -50.0 для отрицательных)
- Обработка NaN значений

---

### 2. Усиление штрафов за малое количество сделок

**Проблема:** Мультипликативные штрафы `Infinity * 0.5 = Infinity` не работают.

**Решение:**

```python
def _apply_trade_count_penalties(self, metrics):
    """ЖЁСТКИЕ штрафы за малое количество сделок"""
    trades = metrics["total_trades"]
    
    # Аддитивные штрафы (работают даже с Infinity)
    penalty = 0
    
    if trades < 5:
        penalty += 1000  # Очень жёсткий штраф
    elif trades < 10:
        penalty += 500
    elif trades < 20:
        penalty += 200
    elif trades < 30:
        penalty += 100
        
    # Мультипликативные штрафы (дополнительно)
    multiplier = 1.0
    if trades < 10:
        multiplier *= 0.1  # Сильное уменьшение
    elif trades < 20:
        multiplier *= 0.3
    elif trades < 30:
        multiplier *= 0.6
        
    return penalty, multiplier
```

**Ключевые моменты:**
- Аддитивные штрафы работают даже с Infinity
- Комбинация аддитивных и мультипликативных штрафов
- Жёсткий минимум: `trades < 5` → `return -float('inf')`

---

### 3. Ограничение search space для Long/Short параметров

**Проблема:** Слишком широкие диапазоны позволяют ГА найти "лазейку".

**Решение в `config/ga_config.yaml`:**

```yaml
long_signal_multiplier:
  type: float
  min: 0.8    # Было 0.5 - ограничиваем снизу
  max: 1.2    # Было 1.5 - ограничиваем сверху

short_signal_multiplier:
  type: float  
  min: 0.8    # Было 0.5
  max: 1.2    # Было 1.5

entry_threshold_long:
  type: float
  min: 0.5
  max: 0.7    # Было 0.8 - делаем более строгим

entry_threshold_short:
  type: float
  min: 0.4    # Было 0.3 - повышаем минимальный порог
  max: 0.6
```

**Дополнительные constraints (в коде):**
- `long_signal_multiplier + short_signal_multiplier >= 1.6` (предотвращает одновременное ослабление)
- `entry_threshold_long >= entry_threshold_short` (Long входы строже)

---

### 4. Полная улучшенная fitness функция

**Структура:**

```python
def _calculate_fitness_v2(self, metrics):
    """Улучшенная fitness функция с защитой от переобучения"""
    
    # 1. Предварительная валидация метрик
    val_metrics = self._validate_metrics(metrics.get("val"))
    train_metrics = self._validate_metrics(metrics.get("train"))
    
    # 2. Жёсткие проверки (hard constraints)
    if not self._passes_hard_constraints(val_metrics):
        return -float('inf')
    
    # 3. Безопасные метрики
    safe_val = self._get_safe_metrics(val_metrics)
    safe_train = self._get_safe_metrics(train_metrics)
    
    # 4. Основной score на validation
    base_score = self._calculate_base_score(safe_val)
    
    # 5. Штрафы за переобучение и нестабильность
    penalties = self._calculate_all_penalties(safe_train, safe_val)
    
    # 6. Итоговый fitness
    final_score = base_score - penalties
    
    # 7. Гарантия конечного значения
    if np.isinf(final_score) or np.isnan(final_score):
        return -1000.0
        
    return final_score
```

**Жёсткие constraints:**

```python
def _passes_hard_constraints(self, metrics):
    """Жёсткие ограничения - отсекаем плохие решения"""
    trades = metrics["total_trades"]
    
    # Минимум 10 сделок на валидации
    if trades < 10:
        return False
        
    # Максимальная просадка не более 20%
    if metrics["max_drawdown"] > 0.20:
        return False
        
    # Win rate не менее 25%
    if metrics["win_rate"] < 0.25:
        return False
        
    return True
```

---

### 5. Обработка edge cases в метриках

**Валидация метрик:**

```python
def _validate_metrics(self, metrics):
    """Валидация и очистка метрик"""
    if not metrics:
        return self._empty_metrics()
    
    # Замена бесконечностей
    metrics["profit_factor"] = self._safe_metric_value(
        metrics["profit_factor"], "profit_factor", 0.0
    )
    
    metrics["sharpe_ratio"] = self._safe_metric_value(
        metrics["sharpe_ratio"], "sharpe_ratio", 0.0
    )
    
    # Обработка нулевой просадки
    if metrics["max_drawdown"] == 0:
        metrics["max_drawdown"] = 1e-5
        
    # Гарантия, что win_rate в [0, 1]
    metrics["win_rate"] = max(0.0, min(1.0, metrics["win_rate"]))
    
    return metrics
```

---

## 🎯 ОТВЕТЫ НА КОНКРЕТНЫЕ ВОПРОСЫ

### 1. Почему добавление Long/Short параметров ухудшило результаты?

**Причина:** 
- Слишком широкие диапазоны позволили ГА найти "лазейку"
- `long_signal_multiplier: 0.662 + short_signal_multiplier: 0.565 = оба ослаблены`
- В сочетании с узким временным окном это дало 1 сделку на валидации

**Решение:** 
- Ограничить диапазоны: `[0.8, 1.2]` вместо `[0.5, 1.5]`
- Добавить constraint: `long_multiplier + short_multiplier >= 1.6`

---

### 2. Как усилить штрафы за малое количество сделок?

**Текущая проблема:** 
- Мультипликативные штрафы `Infinity * 0.5 = Infinity` не работают

**Решение:**
- Добавить аддитивные штрафы: `trades < 10` → `-500`
- Добавить жёсткие constraints: `trades < 10` → `return -float('inf')`
- Комбинация обоих подходов

---

### 3. Как обрабатывать edge cases?

**Решения:**
- **1 сделка:** Отсекать через hard constraints (`trades < 10` → `False`)
- **Нулевая просадка:** Заменять на `1e-5`
- **Profit Factor = Infinity:** Ограничивать сверху значением `10.0`
- **Sharpe = Infinity:** Ограничивать значением `50.0`

---

### 4. Как ограничить search space?

**Рекомендации:**
- Узкие, более разумные диапазоны:
  - `long_signal_multiplier: [0.8, 1.2]`
  - `short_signal_multiplier: [0.8, 1.2]`
  - `entry_threshold_long: [0.5, 0.7]`
  - `entry_threshold_short: [0.4, 0.6]`

**Constraints:**
- `long_signal_multiplier + short_signal_multiplier >= 1.6`
- `entry_threshold_long >= entry_threshold_short`

---

## 🚀 ПЛАН ИСПРАВЛЕНИЙ

### Немедленные действия (1-2 часа):

1. ✅ Добавить безопасные метрики в `genetic_optimizer.py`
2. ✅ Ввести жёсткие constraints на минимальное количество сделок
3. ✅ Ограничить диапазоны Long/Short параметров в конфиге
4. ✅ Добавить обработку edge cases

### Тестирование (30 минут):

5. ⏳ Запустить быстрый прогон (20×10) для проверки исправлений
6. ⏳ Убедиться, что fitness больше не равен Infinity
7. ⏳ Проверить, что ГА не находит решения с < 10 сделками

### Полный прогон (если тесты успешны):

8. ⏳ Запустить полный ГА (100×100) с исправлениями
9. ⏳ Сравнить результаты с ГА №3

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После исправлений ожидаем:

- ✅ Fitness ≠ Infinity — всегда конечные значения
- ✅ Минимум 10+ сделок на валидации
- ✅ Улучшение баланса Long/Short за счёт ограниченных диапазонов
- ✅ Более стабильные метрики между train/val/test

---

## ⚠️ ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

1. **Рассмотрите использование Sortino ratio вместо Sharpe** — менее чувствителен к выбросам
2. **Добавьте метрику Expectancy** = (Win% × Avg Win) - (Loss% × Avg Loss)
3. **Внедрите walk-forward валидацию** для более надёжной оценки стабильности

---

**Вывод:** Реализуйте эти исправления и проблема с fitness=Infinity и переобучением должна быть решена!

