# 📋 ЗАПРОС К DEEPSEEK: Проблема с fitness=Infinity и переобучением после добавления Long/Short параметров

**Дата:** 18 ноября 2025  
**Тема:** Критическая проблема с fitness функцией и переобучением после расширения search space  
**Контекст:** Проект генетической оптимизации торговых индикаторов для криптовалютного рынка (BTCUSDT)

---

## 🎯 ЦЕЛЬ ЗАПРОСА

Получить рекомендации по решению проблемы с fitness=Infinity и явным переобучением, возникшей после добавления параметров Long/Short балансировки в search space генетического алгоритма.

**Критичные вопросы:**
1. Как исправить проблему с fitness=Infinity из-за бесконечных метрик (Sharpe/PF)?
2. Как усилить штрафы за малое количество сделок, чтобы ГА не находил "хитрые" решения?
3. Как правильно обрабатывать edge cases (1 сделка, нулевая просадка) в fitness функции?
4. Почему добавление Long/Short параметров привело к такому ухудшению результатов?
5. Как правильно ограничить search space, чтобы избежать подобных проблем?

---

## 📊 ТЕКУЩАЯ ПРОБЛЕМА

### Результаты ГА №4 (с Long/Short параметрами)

**Конфигурация:**
- Population: 100
- Max generations: 100 (досрочная остановка на поколении 4)
- Fitness: **Infinity** (целевой 200.0 "достигнут" из-за бесконечных метрик)
- Параллелизация: 4 процесса (pathos)
- **Новые параметры:** Long/Short балансировка включена

**Найденные параметры (проблемные):**
- RSI период: 24
- Stop Loss: 1.88%
- Take Profit: 3.90%
- ATR период: 18
- ATR Stop Multiplier: 2.32
- ATR Trailing Multiplier: 1.48
- WOBI вес: 0.31
- **Временное окно: 05:00–10:00 UTC (5 часов)**
- **Long multiplier: 0.662** (ослаблен)
- **Short multiplier: 0.565** (ослаблен)
- **Entry threshold Long: 0.511** (мягкий)
- **Entry threshold Short: 0.562** (строгий)

**Метрики по срезам (КРИТИЧЕСКАЯ ПРОБЛЕМА):**

| Split | Return | Sharpe | Max DD | Win Rate | Profit Factor | Trades |
|-------|--------|--------|--------|----------|---------------|--------|
| Train | **-8.60%** | **-57.47** | 8.60% | **16.7%** | **0.25** | **18** |
| Val | **+0.04%** | **0.0** | **0.0%** | **100%** | **∞** | **1** ⚠️ |
| Test | **-1.55%** | **-202.47** | 1.55% | **0.0%** | **0.0** | **2** |
| Full | **-9.98%** | **-61.24** | 9.98% | **19.0%** | **0.23** | **21** |

### Критичные проблемы

1. **Fitness = Infinity:**
   - Произошло из-за `profit_factor = Infinity` на валидации
   - Причина: всего **1 сделка** на валидации, которая была прибыльной
   - Нулевая просадка (`max_drawdown = 0.0`) также дала проблемы в расчётах

2. **Явное переобучение:**
   - Train: -8.60% (Sharpe -57.47) ❌
   - Val: +0.04% (Sharpe 0.0, но всего 1 сделка!) ⚠️
   - Test: -1.55% (Sharpe -202.47) ❌
   - Разница Train/Val: **8.64%** (огромная!)

3. **Критически мало сделок:**
   - Val: **1 сделка** (недостаточно для статистики)
   - Test: **2 сделки** (недостаточно для статистики)
   - Train: **18 сделок** (мало для обучения)

4. **Плохие метрики:**
   - Win Rate на Train: **16.7%** (очень низкий)
   - Win Rate на Test: **0.0%** (все сделки убыточные)
   - Profit Factor на Train: **0.25** (очень плохо)
   - Profit Factor на Test: **0.0** (все сделки убыточные)

---

## 🔍 СРАВНЕНИЕ С ПРЕДЫДУЩИМ УСПЕШНЫМ ЗАПУСКОМ

### ГА №3 (успешный, БЕЗ Long/Short параметров)

**Метрики:**

| Split | Return | Sharpe | Max DD | Win Rate | Profit Factor | Trades |
|-------|--------|--------|--------|----------|---------------|--------|
| Train | **+6.64%** | **7.89** | 5.74% | 41.6% | 1.13 | 161 |
| Val | **+7.36%** | **42.65** | 2.77% | 46.7% | **2.06** | 30 |
| Test | **+4.02%** | **26.27** | 3.06% | **53.6%** | **1.48** | 28 |
| Full | **+19.09%** | **15.02** | 5.74% | 43.8% | 1.28 | 219 |

**Fitness:** 200.85 (нормальный, не бесконечный)

**Ключевые отличия:**
- ✅ Нет переобучения: Val метрики лучше Train
- ✅ Достаточно сделок: 161/30/28 (статистически значимо)
- ✅ Нормальные метрики: Win Rate 41-54%, PF > 1
- ✅ Стабильность: Sharpe стабилен между срезами

---

## 🔍 ТЕКУЩАЯ АРХИТЕКТУРА

### Генетический алгоритм

**Search space (после добавления Long/Short параметров):**

**Базовые параметры:**
- `rsi_period`: 10-25
- `stop_loss_pct`: 0.5%-2.0%
- `take_profit_pct`: 1.0%-5.0%
- `atr_period`: 8-28
- `atr_stop_multiplier`: 1.0-3.0
- `atr_trailing_multiplier`: 0.5-2.0
- `wobi_weight`: 0.1-0.5
- `time_filter_enabled`: 0 или 1
- `time_window_start`: 0-23 часа
- `time_window_length`: 1-12 часов

**НОВЫЕ параметры Long/Short (добавлены в этом запуске):**
- `long_signal_multiplier`: 0.5-1.5
- `short_signal_multiplier`: 0.5-1.5
- `entry_threshold_long`: 0.5-0.8
- `entry_threshold_short`: 0.3-0.6

**Fitness функция (текущая реализация):**

```python
def _calculate_fitness(self, metrics):
    val_metrics = metrics.get("val") or self._empty_metrics()
    train_metrics = metrics.get("train") or self._empty_metrics()
    
    # Основной fitness на validation метриках
    pnl_score = val_metrics["total_return"] * 100
    sharpe_score = val_metrics["sharpe_ratio"] * 10  # ПРОБЛЕМА: может быть Infinity
    drawdown_score = (1 - val_metrics["max_drawdown"]) * 100
    win_rate_score = val_metrics["win_rate"] * 100
    profit_factor_score = val_metrics["profit_factor"] * 10  # ПРОБЛЕМА: может быть Infinity
    
    base_score = (
        pnl_score * weights["pnl"] +
        sharpe_score * weights["sharpe_ratio"] +
        drawdown_score * weights["max_drawdown"] +
        win_rate_score * weights["win_rate"] +
        profit_factor_score * weights["profit_factor"]  # Если PF=Infinity, то base_score=Infinity
    )
    
    # Штрафы за переобучение и нестабильность
    overfitting_penalty = self._calculate_overfitting_penalty(train_metrics, val_metrics)
    stability_penalty = self._calculate_stability_penalty(train_metrics, val_metrics)
    stability_bonus = self._calculate_stability_bonus(train_metrics, val_metrics)
    
    score = base_score - overfitting_penalty - stability_penalty + stability_bonus
    
    # Мультипликативные штрафы
    penalties_multiplier = self._apply_penalties(val_metrics)
    # ПРОБЛЕМА: если base_score=Infinity, то даже умножение на 0.5 не помогает
    score *= penalties_multiplier
    
    # Аддитивные бонусы
    bonuses = self._apply_bonuses(val_metrics)
    score += bonuses
    
    return score  # Возвращает Infinity, если profit_factor=Infinity

def _apply_penalties(self, metrics):
    multiplier = 1.0
    if metrics["total_trades"] < 10:
        multiplier *= 0.5  # ПРОБЛЕМА: недостаточно сильный штраф
    if metrics["total_trades"] < 20:
        multiplier *= 0.7
    # ... и т.д.
    return multiplier
```

**Проблемы в текущей реализации:**

1. **Нет проверки на Infinity/NaN:**
   - `profit_factor = Infinity` (когда нет убыточных сделок) → `base_score = Infinity`
   - `sharpe_ratio` может быть Infinity при нулевой волатильности
   - Нет проверки перед вычислением fitness

2. **Штрафы за малое количество сделок недостаточны:**
   - `trades < 10` → `* 0.5` (но если base_score=Infinity, то `Infinity * 0.5 = Infinity`)
   - `trades < 20` → `* 0.7` (тоже не помогает)
   - Нужны более жёсткие штрафы или аддитивные штрафы

3. **Нет обработки edge cases:**
   - 1 сделка на валидации → `profit_factor = Infinity` (нет убытков)
   - Нулевая просадка → проблемы в расчётах
   - Нет минимального порога на количество сделок

---

## ❓ КОНКРЕТНЫЕ ВОПРОСЫ

### 1. Обработка бесконечных метрик

**Проблема:** 
- `profit_factor = Infinity` возникает, когда нет убыточных сделок (деление на 0)
- `sharpe_ratio` может быть бесконечным при нулевой волатильности
- Это приводит к `fitness = Infinity`, что ломает раннюю остановку

**Вопросы:**
- Как правильно обрабатывать edge cases (1 сделка, нулевая просадка, нет убытков)?
- Стоит ли ограничивать метрики сверху (например, `min(profit_factor, 10.0)`)?
- Как правильно вычислять Sharpe при малом количестве сделок или нулевой волатильности?
- Нужно ли добавлять специальные проверки перед вычислением fitness?

### 2. Усиление штрафов за малое количество сделок

**Проблема:**
- Текущие штрафы: `trades < 10` → `* 0.5`, `trades < 20` → `* 0.7`
- Но ГА всё равно находит решения с 1-2 сделками на валидации
- Штрафы недостаточно сильные

**Вопросы:**
- Какие штрафы должны быть для `trades < 5`, `trades < 10`, `trades < 20`?
- Стоит ли делать штрафы аддитивными (вычитание из fitness) вместо мультипликативных?
- Нужно ли добавить жёсткий минимум сделок (например, отбрасывать решения с `trades < 10`)?
- Как правильно балансировать штрафы, чтобы не отсечь хорошие решения с редкими сигналами?

### 3. Почему добавление Long/Short параметров ухудшило результаты?

**Проблема:**
- ГА №3 (без Long/Short): fitness 200.85, хорошие метрики
- ГА №4 (с Long/Short): fitness Infinity, плохие метрики
- Новые параметры, похоже, позволили ГА найти "хитрое" решение

**Вопросы:**
- Может быть, диапазоны новых параметров слишком широкие?
- Стоит ли ограничить диапазоны (например, `long_signal_multiplier: 0.7-1.3` вместо `0.5-1.5`)?
- Нужно ли добавить дополнительные ограничения (например, `long_multiplier + short_multiplier >= 1.5`)?
- Может быть, проблема в том, что ГА может слишком сильно ослабить оба сигнала одновременно?

### 4. Улучшение fitness функции

**Проблема:**
- Текущая fitness функция не справляется с edge cases
- Штрафы за малое количество сделок недостаточны
- Нет проверки на валидность метрик перед вычислением fitness

**Вопросы:**
- Как правильно структурировать fitness функцию для обработки всех edge cases?
- Стоит ли добавить предварительную валидацию метрик (проверка на Infinity, NaN, экстремальные значения)?
- Нужно ли использовать более консервативные метрики (например, Sortino вместо Sharpe)?
- Как правильно комбинировать штрафы (мультипликативные vs аддитивные)?

### 5. Ограничение search space

**Проблема:**
- Расширение search space привело к проблемам
- ГА находит "хитрые" решения, которые не работают на реальных данных

**Вопросы:**
- Как правильно ограничить search space, чтобы избежать подобных проблем?
- Стоит ли добавить constraints (например, минимальное количество сделок как hard constraint)?
- Нужно ли использовать более консервативные диапазоны для новых параметров?
- Как правильно тестировать расширение search space (постепенно или сразу)?

---

## 📊 ДЕТАЛЬНЫЙ АНАЛИЗ ПРОБЛЕМЫ

### Анализ найденного решения

**Параметры, которые привели к проблеме:**

1. **Временное окно: 05:00–10:00 UTC (5 часов)**
   - Очень раннее утреннее время
   - Может быть низкая ликвидность
   - ГА выбрал это окно, чтобы минимизировать количество сделок

2. **Long/Short множители:**
   - `long_signal_multiplier: 0.662` (ослаблен)
   - `short_signal_multiplier: 0.565` (ослаблен)
   - Оба множителя ниже 1.0, что ослабляет оба типа сигналов
   - Это привело к очень малому количеству сделок

3. **Пороги входа:**
   - `entry_threshold_long: 0.511` (мягкий)
   - `entry_threshold_short: 0.562` (строгий)
   - В сочетании с ослабленными множителями это дало очень мало сигналов

**Вывод:** ГА нашёл "хитрое" решение, которое минимизирует количество сделок на валидации, что дало "идеальные" метрики (1 прибыльная сделка, нулевая просадка), но это не работает на реальных данных.

---

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После получения рекомендаций планируем:

1. Исправить fitness функцию для обработки edge cases
2. Усилить штрафы за малое количество сделок
3. Ограничить search space для новых параметров
4. Добавить валидацию метрик перед вычислением fitness
5. Перезапустить ГА с исправлениями

---

## 📎 ПРИЛОЖЕННЫЕ ДОКУМЕНТЫ

При отправке запроса к DeepSeek приложите следующие документы:

### Основные документы с результатами:
1. **`docs/GA_RUN_03_DETAILED_ANALYSIS.md`** — детальный анализ успешного ГА №3
2. **`docs/STAGE0_BASELINE_REPORT.md`** — отчёт со всеми запусками ГА и метриками
3. **`docs/IMPLEMENTATION_LONGSHORT_BALANCE.md`** — описание реализации Long/Short параметров
4. **`docs/ACTION_PLAN_UPDATED.md`** — обновлённый план работы

### Документы по архитектуре и дизайну:
5. **`docs/GENETIC_ALGORITHM_DESIGN.md`** — архитектура генетического алгоритма
6. **`docs/FITNESS_FUNCTION_UPDATE.md`** — описание текущей fitness функции с штрафами за переобучение
7. **`docs/GA_OPTIMIZATION_PLAN.md`** — план оптимизации ГА
8. **`docs/OPTIMIZATION_SUMMARY.md`** — резюме реализованных оптимизаций

### Документы по стратегии:
9. **`docs/TEST_TRADES_PATTERN_ANALYSIS.md`** — анализ паттернов сделок на тестовом срезе
10. **`config/ga_config.yaml`** — конфигурация генетического алгоритма
11. **`config/mvp_strategy_config.yaml`** — конфигурация стратегии

### Дополнительные документы (опционально):
12. **`docs/PROJECT_EXPLAINED_SIMPLE.md`** — простое объяснение проекта
13. **`docs/TECHNICAL_SPECIFICATION.md`** — техническое задание проекта
14. **`README.md`** — общий обзор проекта

### Результаты экспериментов:
15. **`results/ga_best_longshort.json`** — JSON файл с результатами проблемного ГА №4
16. **`results/ga_best.json`** — JSON файл с результатами успешного ГА №3

---

**Спасибо за помощь!** 🙏

