# 📖 Простое объяснение параметров Long/Short балансировки

**Дата:** 18 ноября 2025

---

## 🎯 Зачем это нужно?

Наша стратегия сейчас открывает **99%+ Short позиций** (продажи) и почти не открывает Long позиций (покупки). Это рискованно, потому что:

- Если рынок пойдёт вверх, мы упустим прибыль
- Мы зависим только от одного направления
- Нет диверсификации

Поэтому мы добавили **4 параметра**, которые позволяют генетическому алгоритму **найти баланс** между Long и Short позициями.

---

## 📊 Что делают эти параметры?

### 1. `long_signal_multiplier` (множитель для Long сигналов)

**Диапазон:** от 0.5 до 1.5

**Что это значит:**
- Это **"усилитель"** или **"ослабитель"** для сигналов на покупку
- Если значение **1.0** — сигналы не изменяются
- Если значение **1.5** — сигналы на покупку становятся **в 1.5 раза сильнее**
- Если значение **0.5** — сигналы на покупку становятся **в 2 раза слабее**

**Пример:**
- Индикаторы говорят: "сила сигнала на покупку = 0.6"
- Если `long_signal_multiplier = 1.2`, то финальный сигнал = 0.6 × 1.2 = **0.72** (сильнее!)
- Если `long_signal_multiplier = 0.7`, то финальный сигнал = 0.6 × 0.7 = **0.42** (слабее)

**Зачем это нужно:**
- Если ГА видит, что Long позиции работают хорошо, он может **усилить** их (увеличить множитель)
- Если Long позиции работают плохо, он может **ослабить** их (уменьшить множитель)

---

### 2. `short_signal_multiplier` (множитель для Short сигналов)

**Диапазон:** от 0.5 до 1.5

**Что это значит:**
- То же самое, но для сигналов на **продажу** (Short)
- Если значение **1.5** — сигналы на продажу становятся **в 1.5 раза сильнее**
- Если значение **0.5** — сигналы на продажу становятся **в 2 раза слабее**

**Пример:**
- Индикаторы говорят: "сила сигнала на продажу = -0.5"
- Если `short_signal_multiplier = 1.2`, то финальный сигнал = -0.5 × 1.2 = **-0.6** (сильнее!)
- Если `short_signal_multiplier = 0.8`, то финальный сигнал = -0.5 × 0.8 = **-0.4** (слабее)

**Зачем это нужно:**
- Сейчас у нас 99%+ Short позиций, поэтому ГА может **ослабить** Short сигналы (уменьшить множитель)
- Это поможет открывать больше Long позиций

---

### 3. `entry_threshold_long` (порог входа для Long)

**Диапазон:** от 0.5 до 0.8

**Что это значит:**
- Это **"планка"**, которую должен преодолеть сигнал, чтобы открыть Long позицию
- Если значение **0.8** — нужен **очень сильный** сигнал (строгий порог)
- Если значение **0.5** — нужен **средний** сигнал (мягкий порог)

**Пример:**
- Сигнал на покупку = 0.7
- Если `entry_threshold_long = 0.8` → сигнал **0.7 < 0.8** → **НЕ открываем** Long позицию (слишком слабый)
- Если `entry_threshold_long = 0.6` → сигнал **0.7 > 0.6** → **ОТКРЫВАЕМ** Long позицию (достаточно сильный)

**Зачем это нужно:**
- Если Long позиции работают плохо, ГА может **повысить порог** (например, до 0.8) — открывать только очень сильные Long позиции
- Если Long позиции работают хорошо, ГА может **понизить порог** (например, до 0.5) — открывать больше Long позиций

---

### 4. `entry_threshold_short` (порог входа для Short)

**Диапазон:** от 0.3 до 0.6

**Что это значит:**
- То же самое, но для сигналов на **продажу** (Short)
- Если значение **0.6** — нужен **сильный** сигнал (строгий порог)
- Если значение **0.3** — нужен **слабый** сигнал (мягкий порог)

**Пример:**
- Сигнал на продажу = -0.4 (отрицательный, потому что это продажа)
- Если `entry_threshold_short = 0.5` → сигнал **|-0.4| = 0.4 < 0.5** → **НЕ открываем** Short позицию (слишком слабый)
- Если `entry_threshold_short = 0.3` → сигнал **|-0.4| = 0.4 > 0.3** → **ОТКРЫВАЕМ** Short позицию (достаточно сильный)

**Зачем это нужно:**
- Сейчас у нас 99%+ Short позиций, поэтому ГА может **повысить порог** (например, до 0.6) — открывать только очень сильные Short позиции
- Это поможет уменьшить количество Short позиций и открывать больше Long позиций

---

## 🔄 Как это работает вместе?

### Текущая ситуация (99%+ Short):

1. **Множители:**
   - `long_signal_multiplier` = 0.8 (ослабляем Long)
   - `short_signal_multiplier` = 1.2 (усиливаем Short)

2. **Пороги:**
   - `entry_threshold_long` = 0.65 (строгий порог для Long)
   - `entry_threshold_short` = 0.45 (мягкий порог для Short)

**Результат:** Открывается много Short позиций, мало Long позиций.

---

### Что может сделать ГА:

1. **Увеличить `long_signal_multiplier`** до 1.2-1.5 → Long сигналы станут сильнее
2. **Уменьшить `short_signal_multiplier`** до 0.7-0.9 → Short сигналы станут слабее
3. **Понизить `entry_threshold_long`** до 0.5-0.6 → Легче открывать Long позиции
4. **Повысить `entry_threshold_short`** до 0.5-0.6 → Сложнее открывать Short позиции

**Результат:** Баланс Long/Short 70/30% или лучше!

---

## 📊 Визуальная схема

```
ИНДИКАТОРЫ → СИГНАЛЫ
    ↓
[long_signal_multiplier] → УСИЛИВАЕМ/ОСЛАБЛЯЕМ Long сигналы
[short_signal_multiplier] → УСИЛИВАЕМ/ОСЛАБЛЯЕМ Short сигналы
    ↓
[entry_threshold_long] → ПРОВЕРЯЕМ: достаточно ли сильный Long сигнал?
[entry_threshold_short] → ПРОВЕРЯЕМ: достаточно ли сильный Short сигнал?
    ↓
ОТКРЫВАЕМ ПОЗИЦИЮ (Long или Short)
```

---

## 🎯 Итог

Эти 4 параметра позволяют ГА **автоматически найти баланс** между Long и Short позициями:

- **Множители** — усиливают или ослабляют сигналы
- **Пороги** — определяют, насколько сильный сигнал нужен для входа

ГА будет пробовать разные комбинации этих параметров и выбирать те, которые дают **лучший баланс** и **больше прибыли**.

---

**Простыми словами:**
- **Множители** = "громкость" сигналов (можно сделать громче или тише)
- **Пороги** = "высота планки" (можно поднять или опустить)

Чем выше планка и тише сигнал — тем меньше позиций.  
Чем ниже планка и громче сигнал — тем больше позиций.

ГА сам найдёт оптимальный баланс! 🎯

