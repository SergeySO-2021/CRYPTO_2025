# ✅ Реализация оптимизации Long/Short баланса

**Дата:** 18 ноября 2025  
**Статус:** ✅ Реализовано и протестировано

---

## 🎯 Цель

Добавить возможность оптимизации баланса Long/Short позиций через генетический алгоритм, чтобы решить проблему 99%+ Short позиций.

---

## 📝 Реализованные изменения

### 1. Добавлены параметры в search space

**Файл:** `config/ga_config.yaml`

Добавлены 4 новых параметра в `search_space`:

```yaml
long_signal_multiplier:
  type: float
  min: 0.5
  max: 1.5
  # Множитель для long сигналов (оптимизируется ГА)

short_signal_multiplier:
  type: float
  min: 0.5
  max: 1.5
  # Множитель для short сигналов (оптимизируется ГА)

entry_threshold_long:
  type: float
  min: 0.5
  max: 0.8
  # Порог входа для long позиций (оптимизируется ГА)

entry_threshold_short:
  type: float
  min: 0.3
  max: 0.6
  # Порог входа для short позиций (оптимизируется ГА)
```

### 2. Обновлён метод `_build_strategy_config`

**Файл:** `src/core/genetic_optimizer.py`

Добавлена логика передачи новых параметров в конфигурацию стратегии:

```python
# Long/Short балансировка параметры (оптимизируются ГА)
combination_cfg = cfg.setdefault("combination", {})
if "long_signal_multiplier" in genes:
    combination_cfg["long_signal_multiplier"] = float(genes["long_signal_multiplier"])
if "short_signal_multiplier" in genes:
    combination_cfg["short_signal_multiplier"] = float(genes["short_signal_multiplier"])
if "entry_threshold_long" in genes:
    combination_cfg["entry_threshold_long"] = float(genes["entry_threshold_long"])
if "entry_threshold_short" in genes:
    combination_cfg["entry_threshold_short"] = float(genes["entry_threshold_short"])
```

### 3. Стратегия уже поддерживает эти параметры

**Файл:** `src/strategies/mvp_strategy.py`

Стратегия уже имеет поддержку этих параметров:
- `long_signal_multiplier` и `short_signal_multiplier` используются для умножения combined сигналов
- `entry_threshold_long` и `entry_threshold_short` используются для асимметричных порогов входа

---

## ✅ Тестирование

### Быстрый прогон (20×10)

**Команда:**
```bash
py -3 scripts/run_ga_search.py --population-size 20 --max-generations 10 --output results/ga_test_longshort.json
```

**Результаты:**
- ✅ ГА успешно запустился
- ✅ Новые параметры корректно генерируются и передаются
- ✅ Лучший геном содержит новые параметры:
  - `long_signal_multiplier`: 0.716261
  - `short_signal_multiplier`: 0.819886
  - `entry_threshold_long`: 0.796984
  - `entry_threshold_short`: 0.552297

**Наблюдения:**
- ГА достиг target fitness 200.0 на поколении 5 (early stopping)
- Новые параметры оптимизируются вместе с остальными
- Код работает без ошибок

---

## 📊 Ожидаемые результаты

После полного прогона ГА ожидаем:

1. **Улучшение баланса Long/Short:**
   - Текущее: 99%+ Short позиций
   - Целевое: 70/30% или лучше

2. **Улучшение метрик:**
   - Win Rate Long: > 40%
   - Win Rate Short: остаётся > 45%
   - Общий PnL: потенциально +10-15% за счёт лучшей диверсификации

---

## 🚀 Следующие шаги

1. ✅ Добавить параметры в search space — **готово**
2. ✅ Обновить код для передачи параметров — **готово**
3. ✅ Протестировать на быстром прогоне — **готово**
4. ⏳ Запустить полный прогон ГА (100×100) с новыми параметрами
5. ⏳ Проанализировать результаты и сравнить с предыдущими запусками

---

## 📝 Файлы изменений

- `config/ga_config.yaml` — добавлены 4 новых параметра в search_space
- `src/core/genetic_optimizer.py` — обновлён метод `_build_strategy_config`
- `src/strategies/mvp_strategy.py` — уже поддерживает параметры (без изменений)

---

**Статус:** ✅ Реализация завершена, готово к полному прогону ГА

