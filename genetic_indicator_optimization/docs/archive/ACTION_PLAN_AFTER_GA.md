# План действий после боевого запуска ГА

**Дата:** 17 ноября 2025  
**Статус:** Критично — требуется немедленное исправление переобучения

---

## 🎯 Главная проблема: Переобучение

**Симптомы:**
- Train: -32.1% (Sharpe -63.1)
- Val: +1.7% (Sharpe 9.0) ✅
- Test: +4.2% (Sharpe 24.7) ✅

**Вывод:** Стратегия работает на новых данных, но «запомнила» паттерны train-периода, которые не работают.

---

## 🔴 КРИТИЧНО: Исправить fitness функцию

### Проблема
Текущая fitness функция максимизирует метрики на **train**, что приводит к переобучению.

### Решение 1: Использовать validation для отбора (рекомендуется)

```python
# Вместо:
fitness = calculate_fitness(train_metrics)

# Делать:
fitness = calculate_fitness(val_metrics)  # Оценивать на validation!
```

**Плюсы:**
- ГА будет искать решения, которые работают на новых данных
- Автоматически борется с переобучением
- Простая реализация

**Минусы:**
- Val set становится частью «обучения» (но это нормально для ГА)

### Решение 2: Добавить штраф за переобучение

```python
train_fitness = calculate_fitness(train_metrics)
val_fitness = calculate_fitness(val_metrics)

# Штраф, если train сильно лучше val (переобучение)
overfitting_penalty = max(0, train_fitness - val_fitness) * 0.5

fitness = val_fitness - overfitting_penalty
```

**Плюсы:**
- Учитывает оба сплита
- Более гибкий подход

**Минусы:**
- Нужно подбирать коэффициент штрафа

### Решение 3: Комбинированный подход (лучший)

```python
# Основной fitness на validation
val_fitness = calculate_fitness(val_metrics)

# Штраф за переобучение
train_fitness = calculate_fitness(train_metrics)
stability_penalty = abs(train_fitness - val_fitness) * 0.3

# Штраф, если train намного лучше val
overfitting_penalty = max(0, train_fitness - val_fitness) * 0.5

# Финальный fitness
fitness = val_fitness - stability_penalty - overfitting_penalty

# Бонус за стабильность (если train и val близки)
if abs(train_fitness - val_fitness) < threshold:
    fitness += bonus
```

---

## 🔴 КРИТИЧНО: Улучшить сходимость ГА

### Проблема
Fitness стабилизировался на поколении 5 и не улучшался до конца (поколения 6-50).

### Решение 1: Адаптивная мутация

```python
def adaptive_mutation_rate(generation, max_generations, stagnation_count):
    base_rate = 0.15
    
    # Увеличиваем мутацию при застое
    if stagnation_count > 10:
        return min(0.3, base_rate * (1 + stagnation_count * 0.05))
    
    # Увеличиваем мутацию в конце эволюции
    if generation / max_generations > 0.7:
        return min(0.25, base_rate * 1.5)
    
    return base_rate
```

### Решение 2: Увеличить diversity популяции

```python
# Добавить механизм поддержания разнообразия
def maintain_diversity(population, min_distance=0.1):
    # Удалять слишком похожих особей
    # Или использовать niching/crowding distance
    pass
```

### Решение 3: Расширить search space

**Текущие параметры:**
- rsi_period
- stop_loss_pct
- take_profit_pct
- atr_period
- atr_stop_multiplier
- atr_trailing_multiplier
- wobi_weight

**Добавить:**
- rsi_buy_below (порог покупки)
- rsi_sell_above (порог продажи)
- macd_deadband (мёртвая зона MACD)
- bollinger_weight (вес Bollinger)
- macd_weight (вес MACD)
- entry_threshold (порог входа)
- exit_threshold (порог выхода)
- max_trades_per_day (лимит сделок)
- cooldown_bars (охлаждение между сделками)

---

## 🟡 ВАЖНО: Анализ причин переобучения

### Задача 1: Сравнить сделки train vs val/test

```python
# Загрузить сделки
train_trades = pd.read_csv('results/stage0_baseline/trades_train.csv')
val_trades = pd.read_csv('results/stage0_baseline/trades_val.csv')
test_trades = pd.read_csv('results/stage0_baseline/trades_test.csv')

# Анализ:
# 1. Какие индикаторы давали сигналы в прибыльных сделках val/test?
# 2. Какие индикаторы давали сигналы в убыточных сделках train?
# 3. Есть ли временные паттерны (время дня, день недели)?
# 4. Есть ли ценовые паттерны (тренд, флэт, волатильность)?
```

### Задача 2: Временной анализ

```python
# Разбить train на подпериоды и посмотреть метрики
# Может быть, стратегия работает только в определённые месяцы?
```

### Задача 3: Режимный анализ

```python
# Классифицировать рыночные режимы:
# - Тренд вверх
# - Тренд вниз
# - Флэт
# - Высокая волатильность
# - Низкая волатильность

# Посмотреть, в каких режимах стратегия работает лучше
```

---

## 🟡 ВАЖНО: Оптимизация WOBI отдельно

ГА практически отключил WOBI (вес 0.19). Это может означать:
1. WOBI неэффективен в текущей конфигурации
2. Параметры WOBI (веса глубин, z-score пороги) не оптимизированы

### План:
1. Оптимизировать параметры WOBI отдельно (веса ratio3/5/8/60, z-score window)
2. Затем включить оптимизированный WOBI в общую оптимизацию

---

## 📋 План реализации (по приоритетам)

### Неделя 1: Критичные исправления

**День 1-2: Изменить fitness функцию**
- [ ] Реализовать Solution 3 (комбинированный подход)
- [ ] Протестировать на быстром прогоне (6×3)
- [ ] Запустить полный прогон (40×50)

**День 3-4: Улучшить сходимость**
- [ ] Добавить адаптивную мутацию
- [ ] Расширить search space (добавить 5-7 новых параметров)
- [ ] Протестировать на быстром прогоне

**День 5: Анализ переобучения**
- [ ] Сравнить сделки train vs val/test
- [ ] Временной анализ
- [ ] Документировать находки

### Неделя 2: Важные улучшения

**День 6-7: Оптимизация WOBI**
- [ ] Создать отдельный скрипт для оптимизации WOBI
- [ ] Запустить оптимизацию
- [ ] Интегрировать лучшие параметры в общую оптимизацию

**День 8-9: Улучшение риск-менеджмента**
- [ ] Эксперименты с динамическими стопами
- [ ] Тестирование trailing stop с разными множителями
- [ ] Добавление перевода в безубыток

**День 10: Расширение метрик fitness**
- [ ] Добавить штрафы за turnover
- [ ] Добавить штрафы за нестабильность
- [ ] Протестировать на быстром прогоне

---

## 🎯 Ожидаемые результаты после исправлений

1. **Устранение переобучения:**
   - Train и Val метрики должны быть близки
   - Test метрики должны оставаться положительными

2. **Улучшение сходимости:**
   - Fitness должен улучшаться на протяжении всех поколений
   - Больше разнообразия в популяции

3. **Более стабильная стратегия:**
   - Меньше разброс между сплитами
   - Более предсказуемые результаты

---

## 📊 Метрики успеха

После исправлений ожидаем:
- **Train Return:** -5% до +5% (вместо -32%)
- **Val Return:** +1% до +5% (остаётся положительным)
- **Test Return:** +2% до +6% (остаётся положительным)
- **Разница Train-Val:** < 10% (вместо 33%)
- **Sharpe (Val):** > 5 (остаётся хорошим)
- **Sharpe (Test):** > 10 (остаётся хорошим)

---

**Следующий шаг:** Начать с изменения fitness функции (День 1-2).

